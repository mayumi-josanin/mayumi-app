# CLAUDE.md — mayumi-app 開発規律

このリポジトリで作業するときは、必ずこのファイルに従ってください。
手順は [開発者ガイド](docs/developer_guide.md)、ブランチは [Git 運用ルール](docs/git_workflow.md)。

ビジリス配下を触るときは [bijiris/CLAUDE.md](bijiris/CLAUDE.md) と
[bijiris/WORK_NOTES.md](bijiris/WORK_NOTES.md) も先に読むこと。

## 絶対ルール

1. **`ADMIN_TOKEN_SECRET` を変えない。** 変えると発行済みの札が全部無効になり、
   全員が入り口に戻される。まゆみGASのスクリプトプロパティ。
2. **`clasp deploy` には必ず `-i <デプロイID>` を付ける。** 付けないとURLが変わり、
   全アプリがバックエンドを見失う。
3. **配信ファイルを変えたら Service Worker のキャッシュ名を上げる。**
   上げ忘れると、お客様の端末に古い画面が残り続ける。
   **あわせて `index.html` の `?v=` も上げる。**まゆみ側は
   `app.js?v=20260813-3` が3日間据え置かれ、直したのに古いままの端末があった。
4. **ログインは入口（`start/`）だけ。** 各アプリに認証画面を作らない。
   入れない方には入口へご案内する。
5. **`main` へ直接 push しない。** `develop` で確認してからマージする。
6. **アンケートの設問は2か所そろえる。** `bijiris/default-surveys.js` と `bijiris/gas/Code.gs`。
7. **消す前に控えを取る。** 実データを消す関数は、必ずスプレッドシートのコピーを先に作る。
8. **秘密情報をコードに書かない。** スクリプトプロパティで管理する。
9. **お客様の入り直しを発生させない。** 札の形式・会員ID・画面のURL を変えない。

## 変更のたびに考えること

**「これはお客様の画面に出るか？」**

| 触ったもの | お客様への影響 |
|-----------|--------------|
| `develop` のフロント | **無い**（`main` に出すまで） |
| `clasp push`（GAS） | **ある**（トリガー処理は即座に変わる） |
| `clasp deploy`（GAS） | **ある**（アプリの通信先が変わる） |
| スプレッドシートの行の削除 | **ある**（実データ） |

**GAS とスプレッドシートに「開発用」は無い。**触った瞬間が本番。

## 書き方

- **コメントは日本語で、「なぜ」を書く。** 何をしているかはコードを読めば分かる。
  背景・過去の失敗・やらなかった理由を残す。
- **お客様に見える文言は、専門用語を使わない。**
  「トークン」「セッション」「同期」ではなく「合鍵」「ログイン」「記録合わせ」。
- **エラーは、次に何をすればよいかまで書く。**
  「失敗しました」で終わらせない。

## やってはいけないこと

- `clasp push` しただけで「反映済み」と言う（Webアプリはデプロイが要る）
- 「新しいデプロイ」でデプロイする（URLが変わる）
- `bijiris/deploy.sh` を使う（未確認の作業まで `main` へ公開される）
- 既存の保存項目を、断りなく変更・削除する
- 確認していないのに「動きます」と言う
- **別の表に記録が無いことを、その行が空である根拠にする。**
  会員データの「テスト」を「ビジリスに記録が無いから空の行」と判断しかけたが、
  実際には実在の電話番号・住所・生年月日・管理者権限・登録済みのiPhoneが入っていた
  （2026-08-16）。**消す対象は、その行の全項目を見てから判断する。**
- **お名前でお客様の記録を探す。**会員番号（`MYM-####`）で探すこと。
  お名前は同姓同名・改名・表記ゆれで別の方に行き着く。実際に、別名に他の方が
  紛れ込んで記録が混ざった（2026年8月）。お名前は画面に出す表示にだけ使う。
- **記録を読む通信で書き込む。**読むだけの経路で保存すると、項目の引き継ぎ忘れが
  お客様の記録の消失として表に出る。受付で入れたスタンプが、お客様がアプリを
  開いた瞬間に 0 に戻っていたのがこれ（2026年8月）。書くのはログインと保存だけ。
  `bijiris/gas/設定の下見.gs` の `読むだけか確かめる()` で見張れる。

## 調べる道具

`bijiris/gas/` に読み取り専用の道具がある。**推測する前に数える。**
一覧は [開発者ガイド](docs/developer_guide.md#7-調べるための道具gas)。

サーバー（PostgreSQL）に移した記録からも数えられる。
お客様がどこで困っているかは [アプリに戻れない方の調べ方](docs/アプリに戻れない方の調べ方.md)。

**サーバーPCへスクリプトを流すときは ASCII だけで書く。**
PowerShell のパイプは日本語を壊し、変数名も文字列も `?` になって `SyntaxError` になる。
日本語は `\uXXXX` のエスケープで渡す。`shell -c '...'` は引用符が外れるので使えない。
`<` のリダイレクトも使えない（`Get-Content | ...` を使う）。

## 直す前に、いつ直ったかを調べる

**「壊れている」と見えたものが、すでに直っていることがある。**

- 復元の失敗を調べたら「姓と名の間の空白で弾かれている」ように見えたが、
  `ecf7140`（2026-08-13）で既に直っていた。修正後の失敗はゼロ。
  気づかずにエラー表示を足していたら、**いま通っている入力を通らなくしていた**（2026-08-18）
- `bijiris/WORK_NOTES.md` の「未反映」も、実際には本番に入っていた（2026-08-18）

記録や見た目で判断せず、**`git log -S` でいつ入ったかを調べ、本番と突き合わせる。**
