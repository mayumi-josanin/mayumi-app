[project]
name = "mayumi-server"
version = "0.1.0"
description = "まゆみ助産院アプリのデータベース（PostgreSQL + Django）"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<5.3",
    "psycopg[binary]>=3.1",
    "gunicorn>=22.0",
    "whitenoise>=6.5",
]

[tool.setuptools]
py-modules = []
