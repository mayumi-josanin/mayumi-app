# 段階B の下ごしらえ — 正をサーバーへ移すには何が要るか

段階A（写しをサーバーへ）は 2026-08-18 に完了した。1,751件が入っている。
**ただし正はまだスプレッドシートで、アプリは GAS を見ている。**

段階Bは「どちらを正とするか」を切り替える段階。
**お客様の画面に影響が出る**ので、まず何が足りないかを数えた（2026-08-21）。

---

## いまの落差

| | 数 |
|---|---|
| GAS が受けている種類 | **99** |
| 管理アプリが投げる種類 | 37 |
| お客様アプリが投げる種類 | 12 |
| **サーバーが出せるもの** | **3**（health / entry-info / measurements） |

**サーバー側はまだ 3 つしか無い。**データは全部あるが、
アプリが話しかける口がほとんど無い。

> 99すべてを作る必要は無い。使われていないもの・管理アプリ専用のものがある。
> だが**お客様アプリの12種＋読み取りは、切り替えるなら必ず要る。**

## お客様アプリが使う12種（切り替えに必須）

    cancel                    注文の取り消し
    confirmReceipt            受け取り確認
    drawRewardGacha           特典のガチャ
    order                     注文
    recoverAccount            **復元**
    removeUserDeviceSession   端末の解除
    resetForgottenPasscode    **パスコードの再設定**
    syncUserDeviceSession     端末の記録合わせ（自動）
    syncUserRewardStatus      特典状態の同期（自動）
    unsubscribePush           通知の解除
    updateUser                会員情報の編集
    uploadImage               画像の保存

このうち **recoverAccount と resetForgottenPasscode は入り口そのもの。**
ここを間違えると、お客様が入れなくなる。

**syncUserDeviceSession と syncUserRewardStatus は自動処理で、
93日で8,579回**呼ばれている。切り替えたら、その負荷がサーバーに来る。

---

## 進め方の案（まだ決定ではない）

**一度に全部は切り替えない。**読むだけのものから、少しずつ移す。

| 段 | 何を | なぜ先か |
|---|---|---|
| B-1 | **読むだけ**（お知らせ・カレンダー・メニュー・商品・FAQ） | 壊れても表示が古くなるだけ。お客様は入れる |
| B-2 | 会員の**読み取り**（getInitialData など） | まだ書き込まないので、戻すのが楽 |
| B-3 | **書き込み**（updateUser・注文・スタンプ） | ここから先は戻すのが難しい |
| B-4 | **入り口**（recoverAccount・resetForgottenPasscode） | **最後。**間違えるとお客様が入れなくなる |

各段で必ず決めること:

- **戻し方**。何分で元に戻せるか。戻す判断は誰がするか
- **実施する時間帯**。お客様が使っていない時間
- **確かめ方**。切り替えた直後、何を見て「大丈夫」と判断するか

## 先に片付けておくとよいこと

### ① 写しを最新にする

いまの写しは 8/17〜18 に取り込んだもの。実際のシートとは十数件ずれている
（`gas/写しの突き合わせ.js` で数えられる）。切り替えの直前に一度そろえる。

**操作履歴だけは、そのまま取り込み直してはいけない。**
古い記録の自動削除で先頭が消え、行番号がずれている。
取り込み直すなら先に `records_auditlog` を空にすること。

### ② 「使っていない口」を数える

99のうち、実際に呼ばれているのはいくつか。操作履歴（`records_auditlog`）に
種別が残っているので数えられる。**作らなくてよいものが分かれば、仕事が減る。**

### ③ 札（トークン）の突き合わせ

[移行設計](移行設計.md#入り直しをゼロにするための約束)のとおり、
**GASが作った札を、サーバー側が同じと判定するか**を実データで確かめる。
これが合わない限り切り替えない。`ADMIN_TOKEN_SECRET` は変えない。

---

## いま言えること

**データは揃っている。足りないのはアプリとサーバーをつなぐ口。**
段階Bは「移行」というより「作り直し」に近い分量がある。

急ぐ理由は無い。サーバーは写しのまま置いておいても、
控えが毎日取れて、見張りが動いている。**止まってもお客様には何も起きない。**

---

## ① 使っていない口を数えた（2026-08-21）

**操作履歴（`records_auditlog` 794件・2026-05-17〜08-17）から数えた。**
GAS は5種類だけ記録から外している（`AUDIT_LOG_SKIP_TYPES`）ので、
**それ以外は呼ばれれば必ず残る。**つまり「一度も出てこない＝93日間使われていない」。

| | 数 |
|---|---|
| GAS が受ける種類（重複を除く） | **96** |
| 記録から外れている | 5 |
| 記録に出うる | 91 |
| **実際に出てきた** | **28** |
| **93日間で一度も呼ばれていない** | **63** |

### 実際に使われた28種（多い順）

    updateUser 155 / recoverAccount 140 / addBlog 67 / saveMenuRevenueRecord 61
    updateAdminRewardStatus 59 / resetForgottenPasscode 49 / deleteUser 42
    addCalendar 40 / deleteOrders 30 / updateBlog 29 / drawRewardGacha 27
    saveProductRevenueRecord 16 / updateCalendar 13 / mergeUsers 13 / deleteRow 10
    updateMenu 8 / saveRewardGachaConfig 8 / grantSurveyStamp 7
    runManualBackup 3 / restoreDeletedRecord 3 / issueTransferCode 2 / addMenu 2
    deleteProductRevenueRecord 2 / deleteMenuRevenueRecord 2 / deleteRows 1
    deleteCategory 1 / addCategory 1 / updateProduct 1
    パスコード再設定 2（GASの一覧に無い日本語の種別。別経路で書かれている）

### 読み取りは、この数え方では分からない

**`getNews` などの読み取りが「一度も出てこない」のは、使われていない意味ではない。**
記録されるのは `doPost` で、読み取りの多くは別経路（`doGet` / `action=`）を通る。
実際、お客様アプリは起動のたびに `getInitialData` を呼んでいるはずだが、
記録には1件も無い。**読み取り側は、別の方法で数える必要がある。**

### それでも分かったこと

**書き込み側は28種で足りる。**63種のうち多くは管理アプリの読み取りで、
段階Bでは後回しにできる。**99すべてを作る必要は無い。**

とくに**お客様アプリの書き込みは、実質7種**しか使われていない。

    updateUser / recoverAccount / resetForgottenPasscode / drawRewardGacha
    order / cancel / confirmReceipt

（`syncUserDeviceSession` と `syncUserRewardStatus` は記録外だが、
93日で8,579回。**最も呼ばれる2種**なので当然要る。）

### 次に数えること

**読み取りが実際にどれだけ呼ばれているか。**
操作履歴では分からないので、GAS 側に一時的に数える仕掛けを入れるか、
アプリのコードから「起動時に何を呼ぶか」を追う。


---

## ② 読み取りを数えた（2026-08-21）

操作履歴では分からないので、**アプリのコードから直接拾った。**
読み取りは `getFromGAS(action)` / `fetchFromGAS(action)` を通る。

### お客様アプリが読むもの — **12種**

| action | どこで |
|---|---|
| `getAppRuntimeConfig` | 起動時 |
| `getProducts` | 商品一覧 |
| `getNews` | お知らせ |
| `getPushNotices` | 通知の一覧 |
| `getRewardGachaConfig` | 特典ガチャ |
| `getUserRewardStatus` | 特典状態（会員ごと） |
| `getCalendar` | カレンダー |
| `getCustomerOrders` | 注文履歴（会員ごと） |
| `getSupportFaq` | 使い方チャット |
| `getUserDevices` | 端末の一覧（会員ごと） |
| `getRecoveryCandidates` | **復元の候補** |
| `getMenus` | メニュー一覧 |

### 管理アプリが読むもの — **25種**

    getAdminMenus / getAdminProducts / getAdminUsers / getAdminOrders
    getAdminCalendar / getAdminBlogs / getCategories / getAdminUserOrders
    getPushNotices / getProductRevenueRecords / getMenuRevenueRecords
    getAnalytics / getAdminSupportFaq / getSupportChatAnalytics
    getRewardGachaConfig / getPushUsers / getBackupStatus / getAppRuntimeConfig
    getAdminTrashItems / getAdminTemplates / getAdminSecurityConfig
    getAdminDashboardData / getAdminAuditLogs
    getFirebasePresenceToken / getFirebasePresenceAdminConfig

### 分かったこと

**`getInitialData` はどちらのアプリからも呼ばれていない。**
GAS は受け付けるが、実際には使われていない古い口。**作らなくてよい。**

（①で「記録に0件だから使われているはず」と考えたのは早合点だった。
　**本当に使われていなかった。**コードを見るまで断定しなくてよかった。）

---

## ①②を合わせた結論：作るのは **44種**

| | 数 |
|---|---|
| GAS が受ける種類 | 96 |
| **お客様アプリが使う（書き9＋読み12）** | **21** |
| **管理アプリが使う（書き…＋読み25）** | — |
| **合わせて実際に使われている** | **44**（重複を除く） |
| **どこからも呼ばれていない** | **52** |

**半分以上は作らなくてよい。**

### 切り替えの最小構成 — お客様アプリの21種

**まずここだけ作れば、お客様の側は切り替えられる。**管理アプリは
スプレッドシートを見続けてよい（院内でしか使わないので、多少遅くても困らない）。

    書き込み9  updateUser / recoverAccount / resetForgottenPasscode
              drawRewardGacha / order / cancel / confirmReceipt
              syncUserDeviceSession / syncUserRewardStatus
    読み取り12 getAppRuntimeConfig / getProducts / getNews / getPushNotices
              getRewardGachaConfig / getUserRewardStatus / getCalendar
              getCustomerOrders / getSupportFaq / getUserDevices
              getRecoveryCandidates / getMenus

**このうち読み取り12種は、すでにサーバーにデータが揃っている。**
お知らせ95・カレンダー143・メニュー13・商品9・FAQ46・通知96 は移行済み。
**作るのは「出す口」だけで、中身を用意する必要は無い。**

書き込み9種のうち、注文まわり（order / cancel / confirmReceipt）は
**注文管理シートが0件**なので、いま使われていない。実質6種。

> **次にやること: 札（トークン）の突き合わせ。**
> これが合わない限り、何を作っても切り替えられない。


---

## ③ 札（トークン）の突き合わせ（2026-08-21）

**絶対ルール「お客様の入り直しを発生させない」の要。**
発行済みの札はお客様の端末に入っている。切り替えたあとも同じ札が通らないと、
**全員がログイン画面に戻される。**

### 札の作り（`gas/管理者・お客様.js` から読み取った）

```
札  = base64url( JSON )
JSON = { t: 'member', u: 会員ID, exp: 期限(ミリ秒), sig: 署名 }
署名 = base64url( HMAC-SHA256( 'member|会員ID|期限', ADMIN_TOKEN_SECRET ) )
```

管理者の札も同じ仕組みで、署名のもとが `admin|期限` になるだけ。

| | |
|---|---|
| 作る | `makeMemberToken_()` / `makeAdminToken_()` |
| 検証 | `verifyMemberToken_()` |
| 署名 | `adminSign_()` |
| 有効期間（管理者） | 12時間（`ADMIN_TOKEN_TTL_MS`） |

### 確かめたこと

`gas/札を確かめる.js` の `札を確かめる()` で、**存在しない会員ID**
（`MYM-0000-TEST`）と固定の期限で見本の札を作らせた。
実データは変えず、この札では誰もログインできない。

    署名のもと : member|MYM-0000-TEST|1893456000000
    札        : eyJ0IjoibWVtYmVy...（base64url）
    自分で検証  : 通った

**Python 側で札を読めることは確かめた。**

- `base64url` を復号して JSON に戻せる（GAS は `=` を残すので補う必要がある）
- `t / u / exp / sig` が取り出せる
- **署名のもとの文字列を、同じ形に組み立てられる**

### **まだ確かめていないこと（ここが本番）**

**署名そのものが一致するかは、まだ確かめていない。**

理由: **サーバーに `ADMIN_TOKEN_SECRET` が渡っていない。**
いまサーバーが持っている秘密は `DJANGO_SECRET_KEY` だけ。

    docker compose exec web python -c "..."
    → 環境変数の名前: ['DJANGO_SECRET_KEY']

### 次にやること

1. **`ADMIN_TOKEN_SECRET` をサーバーへ渡す**（`.env` に足す）
   - 値は院長が用意する。**私は見ない。**
   - `.env` は `.gitignore` 済み。**コードに書かない。**
   - 過去に画面へ秘密を出してしまった失敗がある（2026-08-16）。
     **必ず `scp` ＋ `-File` で渡し、画面に出さない。**
2. サーバー側に `verify_member_token()` を書く
3. **上の見本の札を実際に検証させて、`MYM-0000-TEST` が返ることを確かめる**
4. さらに **本物の札**（お客様の端末にあるもの）でも1つ確かめる

**3と4が通るまで、段階Bには進まない。**

> `ADMIN_TOKEN_SECRET` は変えない。変えると発行済みの札が全部無効になり、
> 全員が入り口に戻される（CLAUDE.md 絶対ルール1）。


### 結果：**一致した**（2026-08-21 追記 → 2026-08-22 確認）

`ADMIN_TOKEN_SECRET` をサーバーへ渡し、サーバー側で HMAC-SHA256 を計算して
GAS の署名と比べた。

| 署名のもと | GAS | サーバー | |
|---|---|---|---|
| `admin\|1893456000000` | `0bWBVhv3WvGquKq34othsepm5Rdn_nP-eSUifEDFUp4=` | 同じ | **一致** |
| `member\|MYM-0000-TEST\|1893456000000` | `e_LOXY7kgSYmgxnL7CqQv15fzbpijFvTw95O6gMRrvc=` | 同じ | **一致** |

**サーバーは、GASが作った札を同じと判定できる。**
`ADMIN_TOKEN_SECRET` を変えない限り、切り替えてもお客様は入り直しにならない。

#### 途中で1回、間違った結論を出しかけた

最初、会員の札が「署名不一致」と出た。末尾1文字だけ違っていた（`vc=` と `vg=`）。

原因は**私が実行ログの画面から札の文字列を写し取ったときの読み違い**。
`c` と `g` を取り違えていた。秘密も署名の作り方も正しかった。

**管理者の札が完全一致したことで、秘密と作り方が正しいと分かった。**
片方だけで判断していたら「秘密が違う」と誤って結論づけていた。
**2つ以上で確かめる。**

> **画面の文字を人（や私）が写すと読み違える。**
> `l` と `I`、`0` と `O`、`c` と `g`。
> 突き合わせは、写さずに機械同士で比べるか、**別々の2つで確かめる。**

#### サーバーへ渡すには docker-compose.yml も直す必要があった

`.env` に書くだけでは足りない。**`docker-compose.yml` の `environment:` に
列挙されたものだけがコンテナへ渡る。**

```yaml
  # GAS が札（トークン）の署名に使っている秘密。**変えないこと。**
  ADMIN_TOKEN_SECRET: ${ADMIN_TOKEN_SECRET:-}
```

最初これを忘れて `.env` に書いただけで再起動し、
「入っているか: False」となって一度つまずいた。

---

## ③まで終わった。次は？

| | 状態 |
|---|---|
| ① 使っていない口を数える | **済**。96種のうち作るのは44種 |
| ② 読み取りを数える | **済**。お客様アプリは21種で足りる |
| ③ 札の突き合わせ | **済**。**一致した** |
| ④ 読み取り12種の口を作る | これから |
| ⑤ 書き込み6種の口を作る | その後 |
| ⑥ 切り替え | 最後。**戻し方を決めてから** |

**④からは「作る」段階。**ここまでは読むだけだった。


---

## ④ 読み取りの口を作る（2026-08-22 着手）

### 決めたこと：**GASと同じ形にする**

アプリは `GAS_URL + '?action=getNews'` と呼んでいる。
サーバーも**同じ形で受ける**ことにした（院長の判断）。

    切り替え … アプリの GAS_URL を1行変えるだけ
    戻す   … URL を元に戻すだけ（数十秒）

見た目は今風ではないが、**お客様を危険にさらさないことを優先する。**
`/api/news` のような形にすると、アプリ側を12か所以上書き換えることになり、
戻すときも同じだけ大がかりになる。

### 着手して分かったこと：**同じ名前の関数が5つ、二重に定義されている**

**GASは、同じ名前の関数を後から読んだほうで上書きする。**
つまり**前のほうは一度も動いていない。**

| 関数 | 1つ目 | 2つ目（**こちらが動く**） |
|---|---|---|
| `getBlogNews` | 3279行 | **8668行** |
| `getCalendarEvents` | 3344行 | **8292行** |
| `getAdminBlogs` | 4938行 | **8737行** |
| `getAdminCalendar` | 6558行 | **8348行** |
| `handleUpdateOrder` | 3201行 | **4829行** |

**移すときに1つ目を写すと、動きが変わる。**
中身は似ているが同じではない。たとえば `getBlogNews` は、
2つ目だけが `ensureSortOrderColumn_` と `ensureNoticeDeletedAtColumn_` を見ていて、
返す項目にも `rowIdx` `linkUrl` `linkButtonText` が増えている。

> **移す前に必ず「どちらが動いているか」を確かめる。**
> `grep -n "^function 名前"` で行番号を出し、**大きいほうを読む。**

### `getNews` が返す形（実物・8668行）

```json
{ "status": "ok",
  "news": [ { "rowIdx": 2, "date": "…", "title": "…", "category": "…",
              "type": "お知らせ|ブログ", "icon": "📢", "body": "…",
              "image": "…", "imageUrl": "…", "imageUrls": ["…"],
              "updatedAt": "…", "linkUrl": "…", "linkButtonText": "…" } ] }
```

**`image` と `imageUrl` は同じ値を2つ入れている。**古い呼び名を残したまま
新しい名前を足したため。**片方だけにするとアプリが崩れる恐れがあるので、
そのまま両方返す。**


### 1つ目 `getNews` を作った結果（2026-08-22）

`server/apps/gasapi/` を作り、`?action=getNews` で受けられるようにした。
`config/urls.py` では **measurements の後ろに置く**こと。先に置くと
`health` / `measurements` まで拾ってしまう。

突き合わせた結果:

| | GAS | サーバー | |
|---|---|---|---|
| status | ok | ok | 一致 |
| **項目の数** | **16** | **16** | **一致** |
| 項目の名前 | rowIdx / date / title / category / type / icon / body / image / imageUrl / imageUrls / updatedAt / linkUrl / linkButtonText / publishAt / noticeStatus / sortOrder | 同じ | **一致** |
| categories | 7件 | 7件 | 一致 |
| **news の件数** | **88** | **86** | **2件ちがう** |

#### 途中で自分の間違いを1つ直した

最初、除外条件に `published`（公開設定）を入れていた。**GAS は見ていない。**
GAS が見るのは「お知らせ一覧公開」のほうだけ。
入れたままだと、GASでは出ているものがサーバーでは消える。

    GAS（8668行）が外している4つ:
      notice_delisted_at が入っている      （お知らせ一覧削除日時）
      deleted / deleted_at が入っている     （削除状態・削除日時）
      notice_listed が「非公開」            （お知らせ一覧公開）
      publish_at がまだ来ていない           （公開開始日時）

**条件はコードから写す。似ているから同じだろう、で書かない。**

#### 残る2件の差

**まだ合っていない。**サーバーの写しが 8/17 のもので、
シートはその後 95→97行に増えている（8/21 の突き合わせで確認済み）。
**写しが古いことが原因の可能性が高いが、確かめていない。**

次にやること: 写しを最新にしてから、もう一度数える。
それでも合わなければ、**1件ずつ突き合わせて原因を探す。**

> **件数が合うまで、この口は使わない。**
> 2件足りないまま切り替えると、お客様には「記事が2つ消えた」ように見える。


### 2つ目以降を作る前に分かったこと（2026-08-22）

残り11種のうち、**データが揃っているのは思ったより少ない。**
GASの実物を読んで、サーバーに材料があるかを1つずつ確かめた。

| action | 材料 | 作れるか |
|---|---|---|
| `getNews` | お知らせ95 + カテゴリ7 | **作った**（件数の突き合わせ待ち） |
| `getMenus` | メニュー13 | **作れる** |
| `getCalendar` | カレンダー143 | **作れる** |
| `getSupportFaq` | FAQ46 | **作れる** |
| `getPushNotices` | プッシュ通知96 | **作れる** |
| `getProducts` | 商品9 | **足りない**（下記） |
| `getAppRuntimeConfig` | スクリプトプロパティ | **移していない** |
| `getRewardGachaConfig` | スクリプトプロパティ | **移していない** |
| `getUserRewardStatus` | 会員の特典履歴 | 会員データにあるが要確認 |
| `getUserDevices` | 会員の端末セッション | 同上 |
| `getRecoveryCandidates` | 会員データ | 同上 |
| `getCustomerOrders` | 注文 | **注文管理シートは0件** |

#### `getProducts` に足りないもの：**「売切状態」列**

商品マスタには `売切状態` という列があり、GAS はこれを見て
`soldOutStatus` / `isSoldOut` / `isLowStock` を返している。

```js
const soldOutStatus = normalizeProductSoldOutStatus_(row[soldOutCol - 1]);
isSoldOut:  soldOutStatus === '売切',
isLowStock: soldOutStatus !== '売切' && lowStockThreshold > 0
            && stockQty > 0 && stockQty <= lowStockThreshold,
```

**サーバーの `Product` にこの列が無い。**移行のとき（2026-08-17）
`表の下見()` で見た16列に入っていなかったか、見落としたか。
**売切の商品が「在庫あり」として出てしまう。**

対処: 商品マスタをもう一度下見して、`売切状態` 列を移す。

#### 除外条件は、口ごとに違う

同じ「公開」でも見ている列が違う。**まとめて書かない。**

| action | 公開の判定 |
|---|---|
| `getNews` | **お知らせ一覧公開のみ**（公開設定は見ない） |
| `getMenus` | **公開設定が「公開」**であること |
| `getProducts` | **公開設定が「非公開」でない**こと（空欄は公開扱い） |

`getMenus` は「公開でなければ外す」、`getProducts` は「非公開なら外す」。
**空欄のときの扱いが逆になる。**写し間違えると、商品が消えるか、
出してはいけないものが出る。


### 商品マスタをもう一度下見した（2026-08-22）

**9行 × 22列。移行したとき（2026-08-17）は16列だった。6列増えている。**

| # | 列 | 埋まり |
|---|---|---|
| 1〜8 | カテゴリ / 商品名 / 価格（円）/ アイコン / 背景色コード / 公開設定 / 商品説明 / 商品説明画像 | 9件 |
| 9 | 更新日時 | 6件 |
| 10 | 在庫数 | 9件 |
| 11 | 在庫警告閾値 | 5件 |
| 12〜13 | 削除日時 / 公開開始日時 | 0件 |
| 14 | お知らせ一覧公開 | 9件 |
| 15 | 削除状態 | 0件 |
| 16〜18 | 特別価格（円）/ 特別価格開始日時 / 特別価格終了日時 | 0件 |
| 19〜20 | 削除理由 / お知らせ一覧削除日時 | 0件 |
| 21 | お知らせ一覧掲載日時 | 2件 |
| **22** | **売切状態** | **9件**（例: 在庫あり） |

**サーバーに無いのは22列目の「売切状態」。**
16〜18の特別価格まわりは移してあるが、**特別価格開始・終了日時は移していない**
（どちらも0件なので実害は無い）。

> **移行時の16列という記録は、その時点のもの。**
> シートは `ensure〜` で列が増える作りなので、**作る直前にもう一度下見する。**
> 「移したから大丈夫」と思い込むと、**増えた列を黙って落とす。**


### 全6表を点検した結果（2026-08-22）

`gas/列の点検.js` の `列を点検する()` で、シートの見出しを全部出し、
サーバーのモデルと突き合わせた。

| シート | シートの列 | サーバー | |
|---|---|---|---|
| ブログ・お知らせ | 20 | 20 | **一致** |
| カレンダー | 19 | 17 | 下記 |
| MENUS | 16 | 19 | 一致（サーバーが多いのは内部項目） |
| **商品マスタ** | **22** | **22** | **売切状態が無い**（下記） |
| カテゴリマスタ | 2 | 3 | 一致 |
| APP_SUPPORT_FAQ | 7 | 8 | 一致 |
| PUSH_NOTICES | 15 | 16 | 一致 |

**欠けているのは商品マスタの「売切状態」1列だけだった。**

#### カレンダーの19列 vs サーバー17列

シートには **17.リンクURL(0件) / 18.ボタンテキスト(0件)** がある。
**どちらも0件**なので、いま移さなくても実害は無い。
ただし**使われ始めたら移す必要がある。**

#### 数が合っていても中身が違う場合がある

サーバーのほうが多い表（MENUS 16→19 など）は、
`sheet_row` のような**サーバー側だけの内部項目**が入っているため。
**列数を数えるだけでは足りない。名前で突き合わせる。**

#### この点検は毎回やる

商品マスタは5日で16→22列に増えていた。
**「移したから大丈夫」は通用しない。**口を作る直前に、必ずこれを走らせる。


### `getNews` が一致した（2026-08-22 17:52）

写しを最新にしたところ、**GASとサーバーの答えが完全に一致した。**

| | GAS | サーバー | |
|---|---|---|---|
| status | ok | ok | 一致 |
| **news** | **88** | **88** | **一致** |
| categories | 7 | 7 | 一致 |
| 項目の数 | 16 | 16 | 一致 |

**2件の差は、写しが古かっただけだった。**除外の条件は正しかった。

取り込みの結果:

    お知らせ    95 → **97**（+2）
    プッシュ通知  96 → **97**（+1）
    商品       9件すべてに売切状態が入った
    他の表      変化なし

#### 売切の商品が1件あった

院長は「いま売切状態はない」とおっしゃっていたが、**データ上は1件あった。**

    9行 天然だし調味粉  sold_out='売切'  stock=0

在庫0で売切の印が付いている。**ご認識と食い違っているので、
シート側を確かめていただく必要がある**（意図した売切か、
在庫を0にしたときに自動で付いたのか）。

> **「無い」と言われても、データを見る。**
> 見ずに「無いなら急がない」と判断していたら、
> **売切の商品が「在庫あり」として出る不具合を、そのまま残していた。**

#### ④の1つ目が完成した

やり方が固まった。残り4種は同じ手順の繰り返し。

    ① 列を点検する()          … シートの列が増えていないか
    ② GASの実物を読む          … **二重定義に注意。大きいほうを読む**
    ③ 同じ形で返す口を作る       … 除外条件はコードから写す
    ④ 実際に呼んで突き合わせる    … 件数・項目数・項目名
    ⑤ 合うまで直す


### 読み取り5種が GAS と一致した（2026-08-22 17:58）

| action | GAS | サーバー | 項目 | |
|---|---|---|---|---|
| `getNews` | 88 | 88 | 16 / 16 | **一致** |
| `getMenus` | 10 | 10 | 12 / 12 | **一致** |
| `getCalendar` | 120 | 120 | 15 / 15 | **一致** |
| `getSupportFaq` | 46 | 46 | 7 / 7 | **一致** |
| `getPushNotices` | 97 | 97 | 15 / 15 | **一致** |

**件数も項目名も、5種すべて同じ。**

#### 除外条件は、やはり口ごとに違った

    getNews     … お知らせ一覧公開のみ（公開設定は見ない）
    getMenus    … **公開設定が「公開」**であること
    getCalendar … **公開設定が「公開」**であること
    getPushNotices … 削除状態・削除日時のみ（公開の概念が無い）
    getSupportFaq  … 状態が「公開」かつ**質問と回答が両方ある**こと

**まとめて書いていたら、どれかが必ず狂っていた。**

#### 無いものは作らない

カレンダーの `category` `linkUrl` `linkButtonText` は、
シートに列はあるが**サーバーには移していない**（点検で0件と確認済み）。
**推測で値を埋めず、空文字を返す。**GASも実際ほとんど空。

使われ始めたら移す。それまでは空のままでよい。

---

## ④の進み具合

| | 状態 |
|---|---|
| **読み取り 5種** | **済**。GASと完全一致 |
| 読み取り `getProducts` | 材料は揃った（売切状態を移した）。**これから** |
| 読み取り 会員まわり4種 | `getUserRewardStatus` / `getUserDevices` / `getRecoveryCandidates` / `getCustomerOrders` |
| 読み取り 設定2種 | `getAppRuntimeConfig` / `getRewardGachaConfig`（**スクリプトプロパティ。未移行**） |
| 書き込み 6種 | まだ |


### `getProducts` も一致した（2026-08-22 19:30）

| action | GAS | サーバー | 項目 | |
|---|---|---|---|---|
| `getNews` | 88 | 88 | 16 | **一致** |
| `getMenus` | 10 | 10 | 12 | **一致** |
| `getCalendar` | 120 | 120 | 15 | **一致** |
| `getSupportFaq` | 46 | 46 | 7 | **一致** |
| `getPushNotices` | 97 | 97 | 15 | **一致** |
| `getProducts` | 8 | 8 | 17 | **一致** |

**読み取り6種、すべて一致。**

#### ここでも自分の間違いを1つ直した

最初 `getProducts` が **9件**返って、GAS の8件と合わなかった。

原因は**私が GAS に無い条件を足したこと。**
「シートの公開設定が空欄のとき `published` が False になってしまうから」と考え、
`notice_listed` で救う条件を書いていた。**GAS にそんな条件は無い。**

    サーバー  9行 天然だし調味粉  published=False  notice_listed=True
    → 私の条件が非公開の商品を**救ってしまい**、お客様に出るところだった

GAS はこれだけ。

    if (!row[1] || String(row[5]).trim() === '非公開') continue;

**推測で条件を足すと、出してはいけないものが出る。**
「こうなっているはずだから、こう補っておこう」が一番危ない。
**書いてあるとおりに写す。**

> **売切の「天然だし調味粉」は、そもそも非公開だった。**
> だからお客様の画面には出ていない。院長が「売切状態はない」と
> おっしゃったのは、**お客様から見える範囲では正しかった。**

---

## ④の進み具合（2026-08-22 時点）

| | 状態 |
|---|---|
| **読み取り 6種** | **済**。GASと完全一致 |
| 読み取り 会員まわり4種 | `getUserRewardStatus` / `getUserDevices` / `getRecoveryCandidates` / `getCustomerOrders` |
| 読み取り 設定2種 | `getAppRuntimeConfig` / `getRewardGachaConfig`（**スクリプトプロパティ。未移行**） |
| 書き込み 6種 | まだ |

**読み取り12種のうち6種、ちょうど半分が終わった。**


### 会員まわり4種を読んだ（2026-08-22）— **これまでと性質が違う**

読み取り6種は「表を読んで返すだけ」だった。会員まわりは違う。

#### `getUserRewardStatus` は**読み取りではない**

```js
// 満杯で保留になっていたお礼スタンプがあれば、ここで回収する
const redeemed = redeemPendingSurveyStamp_(sheet, rowIdx, row, memberId);
```

`redeemPendingSurveyStamp_` は**シートに書き込む。**

- スクリプトプロパティの「保留」を見る
- あればスタンプを1つ足して**シートを更新**
- 「付与済み」を記録し、「保留」を消す

**名前は get だが、実際は書き込み。**読むだけの口として作ると、
**保留スタンプが永久に回収されなくなる。**

> CLAUDE.md の「記録を読む通信で書き込む」を禁ずる項目は、
> まさにこれと同じ形の不具合（お客様のスタンプが0に戻った）から来ている。
> **ここは意図した書き込みだが、移すときは「読むだけ」ではないと知っておく。**

#### `getRecoveryCandidates` は**認証なしで呼べる**

コードにこう書いてある。

```js
// このAPIは認証なしで呼べる（会員登録の途中で使うため）。
// 生年月日と電話番号は絶対に返さない。生年月日は handleRecoverAccount の
// 必須一致キーなので、返すと氏名だけで他人のアカウントに入れてしまう。
```

守っていること:

| | |
|---|---|
| 返すもの | `memberId` / **伏せたお名前** / 一致した理由 |
| 返さないもの | **生年月日・電話番号・フリガナの中身** |
| お名前 | `maskNameForRecovery_` で「山○○○」の形に |
| 件数 | **上位5件まで** |
| 並び | 一致の重み（氏名・フリガナ2点／電話・生年月日3点）→ 更新日時 |

さらに `RECOVERY_MAX_ATTEMPTS = 10` / 10分の制限がある。

> **生年月日は現実的に1〜2万通りしかない。制限がなければ総当たりで
> 他人のアカウントに入れてしまう**（コードのコメントより）。

**この口をサーバーに作るときは、伏せ字・5件上限・回数制限まで
そのまま写す必要がある。**1つでも抜けると、他人のアカウントに入られる。

#### `getCustomerOrders` は中身が0件

注文管理シートは0行。**作れば必ず空を返す。**
形だけ合わせておけばよい。

#### `getUserDevices` だけが素直

会員IDで1行引いて、端末セッションのJSONを返すだけ。書き込みは無い。

---

### 結論：**会員まわりは、読み取り6種と同じようには作れない**

| action | 性質 | 難しさ |
|---|---|---|
| `getUserDevices` | 読むだけ | **すぐ作れる** |
| `getCustomerOrders` | 読むだけ・0件 | **すぐ作れる** |
| `getUserRewardStatus` | **書き込みを含む** | 保留スタンプの仕組みごと移す必要がある |
| `getRecoveryCandidates` | **認証なしで呼べる** | 伏せ字・5件上限・回数制限を写す必要がある |

**前の2つは今すぐ作れる。後ろの2つは、書き込みの段階（⑤）と一緒に扱うべき。**
読み取りだけ先に切り替えても、この2つは GAS に残さざるを得ない。


### 読み取り8種になった（2026-08-22 20:00）

| action | GAS | サーバー | 項目 | |
|---|---|---|---|---|
| `getNews` | 88 | 88 | 16 | **一致** |
| `getMenus` | 10 | 10 | 12 | **一致** |
| `getCalendar` | 120 | 120 | 15 | **一致** |
| `getSupportFaq` | 46 | 46 | 7 | **一致** |
| `getPushNotices` | 97 | 97 | 15 | **一致** |
| `getProducts` | 8 | 8 | 17 | **一致** |
| `getUserDevices` | — | — | 8 | **一致**（会員IDが要る） |
| `getCustomerOrders` | — | 0 | — | **一致**（必ず空） |

**読み取り12種のうち8種。残るは会員まわり2種と設定2種。**

#### 引数の受け取り方

アプリは `fetchFromGAS(action, params)` で
`?action=... &data={"memberId":"..."}` の形で送ってくる。
`data` の JSON を読む。`?memberId=...` の形でも受けられるようにした。

#### エラーでも 200 で返す

会員IDが無いとき、GAS は

    { status: 'error', message: '会員IDが必要です' }

を **200 で返している。**アプリは `status` を見て判断しているので、
**HTTPの番号で返すと動きが変わる。**文言もそのまま写した。

#### 端末の並びと上限

    最後に使った順（lastSeenAt の新しい順）
    **8件まで**（GAS の MAX_DEVICE_SESSIONS = 8）

`passcodeEnabled` などは **`=== true` のときだけ真**。
「あれば真」にすると、文字列の `"false"` が真になってしまう。

#### `getCustomerOrders` は空でも口を作った

注文管理シートは0行で、サーバーに表も無い（移行設計の判断A「作らない」）。
それでも口は用意した。**無いと 501 を返してしまい、
アプリが「サーバーが壊れている」と受け取る恐れがある。**


### 設定2種も一致した — **読み取り10/12種**（2026-08-23）

| action | GAS | サーバー | |
|---|---|---|---|
| `getAppRuntimeConfig` | 6項目 | 6項目 | **全項目一致** |
| `getRewardGachaConfig` | 4か月 | 4か月 | **一致**（各月4等級・確率合計100） |

**`webBundleVersion` の固定が効いていることを確かめた。**

    保存されている値   2026.04.06.63   ← **使わない**
    GAS が返す値      2026.04.05.61
    サーバーが返す値    2026.04.05.61   ← 一致

#### スクリプトプロパティの移し方

`AppSetting`（鍵と値だけの表）に入れた。中身の形は設定ごとに違うので
JSON のまま持つ。GAS も `JSON.stringify` で入れている。

**秘密は移さない。**`ADMIN_TOKEN_SECRET` などは `.env` にあり、
`AppSetting` には置かない。置くと**控え（バックアップ）に秘密が混ざる。**

書き出し・取り込みの両方で、**移すと決めた2つだけを名指しで通す。**
「全部書き出して後で選ぶ」はしない。書き出した時点で漏れる。

---

## ④の到達点（2026-08-23）

**読み取り12種のうち10種が、GASと完全一致。**

| | |
|---|---|
| `getNews` / `getMenus` / `getCalendar` | 済 |
| `getSupportFaq` / `getPushNotices` / `getProducts` | 済 |
| `getUserDevices` / `getCustomerOrders` | 済 |
| `getAppRuntimeConfig` / `getRewardGachaConfig` | **済** |
| `getUserRewardStatus` | **書き込みを含むので⑤で** |
| `getRecoveryCandidates` | **認証なしで呼べるので⑤で** |

残る2種は、読み取りのついでには作れない。書き込みの段階で扱う。


---

# ここで一区切り（2026-08-23）

## できたこと

| | |
|---|---|
| ① 使っていない口を数える | 96種のうち**作るのは44種**。半分以上は不要 |
| ② 読み取りを数える | お客様アプリは**21種**で足りる |
| ③ 札の突き合わせ | **一致。お客様の入り直しは起きない** |
| ④ 読み取りの口 | **10/12種**が GAS と完全一致 |

**いちばんの関門だった③が通った。**これが合わなければ、
何を作っても切り替えられなかった。

## 途中で見つけて塞いだ落とし穴

| | 見つけ方 |
|---|---|
| 関数の**二重定義が5つ** | `grep -n "^function 名前"` |
| 商品マスタの列が **16→22** に増えていた | `列を点検する()` |
| 除外条件が**口ごとに全部違う** | 実物を1つずつ読んだ |
| `getUserRewardStatus` は**書き込む** | 実物を読んだ |
| `getRecoveryCandidates` は**認証なしで呼べる** | 実物を読んだ |
| 設定の**保存値と返却値が違う** | 書き出して比べた |
| 自分が**GASに無い条件を足していた** | 件数の突き合わせ |

**どれも「実物を読む」「突き合わせる」で見つかった。**
推測で進めていたら、いくつかはお客様の画面に出ていた。

## なぜここで区切るか

**⑤（書き込み）は性質が違う。**

読み取りは、間違えても「表示が変わる」だけで、
**GASと突き合わせれば間違いが分かる。**
実際、上の落とし穴はすべて突き合わせで見つかった。

書き込みは違う。

- **お客様の記録が変わる。**間違えると元に戻せないことがある
- 突き合わせができない。**同じ操作を2回やると、2回書き込まれる**
- `getUserRewardStatus` のように、**読み取りに見えて書くもの**がある

**まとまった時間と、戻し方の相談が要る。**
半端に始めて中断するのが、いちばん危ない。

## いまの状態

    サーバー   HTTP 200・1,756件・読み取り10種が動く
    正        **まだスプレッドシート**（アプリは GAS を見ている）
    お客様     何も変わっていない
    控え      毎日3時＋作業のたび。ドライブへ
    見張り     15分おき

**サーバーは写しのまま。**止まってもお客様には何も起きない。

## 再開するときは

1. `列を点検する()` … シートの列が増えていないか
2. `写しを突き合わせる()` … 写しがどれだけ古いか
3. `GASの答えを数える()` … 作った10種がまだ一致するか
4. それから⑤へ

**⑤に入る前に決めること:**

- 切り替えの順番（読むだけ先か、書き込みも一緒か）
- **戻し方**（何分で戻せるか。戻す判断は誰がするか）
- 実施する時間帯（お客様が使っていない時間）


---

# ⑤（書き込み）の下調べ（2026-08-23）

**まだ作らない。**何をしているかを読んで、決めるべきことを洗い出した。

## 6種の規模と、書き換えるもの

| 関数 | 行数 | 書き換えるもの |
|---|---|---|
| `handleUpdateUser` | 100 | シート / **行追加** |
| `handleRecoverAccount` | 137 | シート |
| `handleResetForgottenPasscode` | 112 | シート |
| `handleDrawRewardGacha` | 66 | シート / **ロック** |
| `handleSyncUserRewardStatus` | 43 | シート |
| `handleSyncUserDeviceSession` | 20 | 直接は書かない（`upsertUserDeviceSession_` が書く） |

## 読んで分かった、注意すべき3つ

### ① `updateUser` は**新規登録も兼ねている**

会員が見つからないと、**新しい行を足す。**

```js
if (targetRow === -1) {
  const rowData = new Array(USER_HEADERS.length).fill('');
  ...
  sheet.appendRow(nextRow);
}
```

**名前は「更新」だが、実際は「無ければ作る」。**
サーバー側で「見つからなければエラー」にすると、**新規登録ができなくなる。**

登録経路も一緒に記録している（`registrationSource` の既定は `'新規登録'`）。

### ② `drawRewardGacha` だけ**ロックを取っている**

```js
const lock = LockService.getScriptLock();
lock.waitLock(10000);
```

理由は明らか。**同じ特典を二重に引かせないため。**
スタンプが10個たまっているかを見て、引いたら消す。
ロックが無いと、**同時に2回押されたときに2回引ける。**

> サーバー側では PostgreSQL の行ロック（`select_for_update`）が同じ役目をする。
> **ロックを写し忘れると、二重に引ける穴が空く。**

### ③ 同じ操作を2回受けたときの扱いが、口ごとに違う

- `syncUserDeviceSession` … **upsert**。同じ端末IDなら上書き。2回来ても安全
- `drawRewardGacha` … **2回引けてはいけない**。ロックで守っている
- `updateUser` … 会員IDで探すので、2回来ても行は増えない（**IDがあれば**）

**通信は失敗して再送されることがある。**
「2回届いても大丈夫か」を、口ごとに確かめる必要がある。

---

## ⑤に入る前に決めること

**この3つが決まらないと着手できない。**

### 1. 切り替えの順番

| 案 | 中身 | 戻しやすさ |
|---|---|---|
| A | 読むだけ先に切り替え、書き込みはGASのまま | **戻しやすい**。ただし読み書きが別の場所を見る期間ができる |
| B | 読み書きまとめて切り替え | 一度で済むが、**戻すときも全部** |

> **Aには落とし穴がある。**読みはサーバー、書きはGAS（＝スプレッドシート）だと、
> 書いた直後にサーバーへ反映されず、**「保存したのに画面が変わらない」**が起きる。
> サーバーの写しを常に最新にする仕組みが要る。

### 2. 戻し方

- **何分で戻せるか。**（URLを戻すだけなら数十秒）
- **戻す判断は誰がするか。**院長か、開発者か
- 戻したあと、**サーバーに書かれた分をどうするか**
  （切り替え中にお客様が保存した記録は、スプレッドシートに無い）

### 3. 実施する時間帯

お客様が使っていない時間。**操作履歴から実際の時間帯を数えられる。**

---

## 進める順番の案

    1. 上の3つを決める（**院長との相談**）
    2. syncUserDeviceSession から作る（20行・upsert・いちばん安全）
    3. updateUser（新規登録も兼ねる点に注意）
    4. drawRewardGacha（**ロックを忘れない**）
    5. recoverAccount / resetForgottenPasscode（**入り口。最後**）

**入り口の2つを最後にする理由:**間違えるとお客様が入れなくなる。
他が全部通ってから、最後に慎重に移す。


## 実施する時間帯 — データから出した答え（2026-08-23）

操作履歴794件を、時間ごとに数えた（お客様の操作だけを抜き出した）。

| 時間 | お客様の操作 |
|---|---|
| 09:00〜15:00 | **1時間あたり29〜57件。ここが本番** |
| 16:00〜23:00 | 3〜15件 |
| **00:00〜06:00** | **合計9件（6時間で）** |
| 深夜2〜3時・5時 | **0件** |

**答え: 深夜2時〜5時。**この帯は3か月で1件も無い。
毎日3時の控えと重ならないよう、**2時台か4時台**がよい。

曜日は 木113 / 土85 / 月77 が多く、**日19 / 水21 / 金22 が少ない。**
（水曜・日曜は休診日と思われる）

> **切り替えるなら、水曜か日曜の深夜2時台。**
> 万一戻すことになっても、翌朝9時までに6時間以上ある。


---

# ⑤の進め方が決まった（2026-08-23）

## 決まった3つ

### 1. 切り替えの順番 → **読み書きまとめて**

読むだけ先に切り替える案は採らない。
**読みはサーバー・書きはGASだと、保存した内容がすぐ画面に出ない。**
「保存したのに変わらない」がお客様に起きる。

まとめて切り替えれば、**アプリの `GAS_URL` を1行変えるだけ**で済み、
**戻すのもURLを戻すだけ**（数十秒）。

### 2. 戻す判断 → **開発者の判断で戻す**

切り替え直後に見守り、異常があれば**即座に戻してから報告する。**
深夜に院長へ確認を取っている間、お客様が困るのを避ける。

**戻す条件をあらかじめ決めておく**（迷わないため）:

| 見えたもの | どうするか |
|---|---|
| 復元・パスコード再設定が失敗する | **即戻す** |
| 画面が真っ白・エラーが出る | **即戻す** |
| 件数がGASと違う | **即戻す** |
| 応答が明らかに遅い（3秒以上） | **即戻す** |
| 見た目が少し違う（並び順など） | 記録して継続。朝に相談 |

### 3. 実施する時間帯 → **水曜か日曜の深夜2時台**

お客様の操作が3か月で0件の帯。毎日3時の控えと重ならない。
戻すことになっても翌朝9時まで6時間以上ある。

---

## 切り替え中にサーバーへ書かれた記録をどうするか

**まだ決めていない。**これが残った宿題。

まとめて切り替えると、その間の保存は**サーバーだけ**に入る。
戻すと、**その記録はスプレッドシートに無い。**

考えられる手当て:

| 案 | 中身 |
|---|---|
| A | 切り替え中の書き込みを**サーバーからGASへも送る**（二重書き） |
| B | 戻したあと、**サーバーの差分を手で写す**（深夜なら数件のはず） |
| C | 戻す前に**サーバーの控えを取り**、あとで突き合わせる |

**深夜2時台なら、3か月の実績で書き込みは0件。**
実質Cで足りる見込みだが、**0件である保証は無い。**

> 次に着手するとき、まずここを決める。


---

# 宿題の答え：切り替え中の書き込みをどうするか（2026-08-23）

## まず、先の分析を訂正する

「深夜2時台はお客様の操作が3か月で0件」と書いたが、**これは不十分だった。**

**操作履歴は自動同期2種を記録していない**（`AUDIT_LOG_SKIP_TYPES`）。
そして自動同期こそが書き込みの大半で、93日で 8,579件ある。
**「操作履歴に0件」は「書き込みが0件」を意味しない。**

## 数え直した

端末の `lastSeenAt`（アプリを開いた時刻）192件を時間ごとに数え、
さらに自動同期と人の操作の比（10.8倍）から、夜間の書き込み回数を見積もった。

| 時間 | 人の操作（93日） | 書き込みの見積もり（1晩あたり） |
|---|---|---|
| 00:00 | 0 | 0.00 |
| 01:00 | 1 | 0.12 |
| **02:00** | **0** | **0.00** |
| **03:00** | **0** | **0.00** |
| 04:00 | 6 | 0.70 |
| 05:00 | 0 | 0.00 |

**4時台の6件の正体:**

    05/20 04:04〜04:06  recoverAccount 失敗 ×5
    07/18 04:01         recoverAccount 成功

**5月20日に1人が5回連続で試しただけ。**毎晩起きることではない。

**02:00〜03:59 なら、1晩あたりの書き込みは実質0.00件。**
30分の切り替えなら、**書き込みが起きる見込みはほぼ無い。**

## 答え：**C（戻す前に控えを取り、あとで突き合わせる）**

二重書き（案A）は、**切り替えのために新しい仕掛けを足すことになる。**
その仕掛け自体が壊れる余地を作る。**得るものより失うものが大きい。**

| やること | |
|---|---|
| 切り替えの**直前**にサーバーの控えを取る | いまの `控えを取る.ps1` で足りる |
| 戻すことになったら、**戻したあとにもう一度控えを取る** | 2つの控えの差が「切り替え中に書かれた記録」 |
| 差があれば、**手でスプレッドシートへ写す** | 実績上0件のはずだが、0件を確かめる |

**「差が無いことを確かめる」ことが目的。**
何もしないのと違うのは、**確かめた記録が残る**こと。

> 30分で0.00件の見込みでも、**0件である保証は無い。**
> だから確かめる。確かめずに「たぶん無い」で済ませない。

---

## ⑤に必要な決定は、これで全部そろった

| | |
|---|---|
| 切り替えの順番 | **読み書きまとめて** |
| 戻す判断 | **開発者の判断で即戻す**（条件は表にした） |
| 時間帯 | **水曜か日曜の 02:00〜03:00** |
| 切り替え中の書き込み | **C。前後で控えを取り、差を確かめる** |

**次は実装。**`syncUserDeviceSession` から作る。入り口2種は最後。


---

# ⑤ 書き込みの1つ目ができた（2026-08-23）

`syncUserDeviceSession` と `removeUserDeviceSession` を作った。
**93日で7,788回**と、いちばん多く呼ばれる書き込み。

## 読み取りと分けて書いた（`writes.py`）

性質が違うから。読み取りは間違えても表示が変わるだけで、
突き合わせれば分かる。**書き込みはお客様の記録が変わる。**

書き込みで守ること:

1. **2回届いても壊れない**（通信は失敗して再送される）
2. **同時に届いても壊れない**（行ロック）
3. GASと同じ答えを返す
4. 書く前に必ず会員を確かめる

## 行ロックを入れた

```python
m = Member.objects.select_for_update().filter(member_id=会員ID).first()
```

**同じ会員に同時に2つ届くと、後から書いたほうが前の端末を消してしまう。**
GAS は1つずつしか動かないので問題にならなかったが、
**サーバーは同時に動く。**ここは写すのではなく、足す必要があった。

## 試した結果（試験用の会員を作り、最後に消した）

| 試したこと | 結果 |
|---|---|
| 同じ端末で2回送る | **1件のまま**（増えない） |
| 2台目を足す | 2件・**新しいほうが `current`** |
| 10台送る | **8件で止まる**（`MAX_DEVICE_SESSIONS` と同じ） |
| 1台外す | 7件 |
| 会員IDなし / 端末IDなし | **GASと同じ文言でエラー** |
| 存在しない会員 | 「会員情報が見つかりません」 |
| 知らない type | 「まだサーバー側にありません」＋ `notImplemented` |

**実データは触っていない。**試験の前後で会員は153名のまま、
試験用の記録も残っていない。控えも先に取った（`mayumi-20260823-2007.dump`）。

## POST の受け方

アプリは `postToGAS({type: '...'})` で JSON を送ってくる。
**同じ形で受ける。**`csrf_exempt` を付けたのは、アプリが別の場所
（GitHub Pages）から呼ぶため。**代わりに合鍵（`API_KEY`）で守っている。**

**エラーでも 200 で返す。**GAS がそうしているから。
知らない type は `notImplemented` を付けて返す。
**黙って成功にしない。**切り替えたときに「書けたつもりで書けていない」を防ぐ。

---

## ⑤の進み具合

| | |
|---|---|
| `syncUserDeviceSession` | **済** |
| `removeUserDeviceSession` | **済**（ついでに作った） |
| `syncUserRewardStatus` | これから |
| `updateUser` | これから（**新規登録も兼ねる**） |
| `drawRewardGacha` | これから（**ロックを忘れない**） |
| `recoverAccount` / `resetForgottenPasscode` | **最後**（入り口） |


---

# ⑤の2つ目：`syncUserRewardStatus`（2026-08-23）

スタンプと特典の状態を、アプリから受け取って記録に反映する。93日で791回。

## **GASと違う動きにした点が2つある**

写すのが原則だが、ここは**写すと危ないと判断した。**

### ① 読めない値が来ても、0にしない

GAS はこう書いている。

```js
stampCount: Math.max(0, Math.min(10, Number(data.stampCount) || 0))
```

`Number(x) || 0` は、**`null`・空文字・読めない文字列が来ると 0 になる。**
**お客様のスタンプが0に戻る。**

> CLAUDE.md の「記録を読む通信で書き込む」を禁ずる項目は、
> まさにこの形の不具合（受付で入れたスタンプが0に戻った）から来ている。

**サーバーでは、読めない値なら「いまの値を残す」。**

| 送られてきたもの | GAS | サーバー |
|---|---|---|
| 何も送らない | いまの値 | いまの値 |
| `null` | **0** | **いまの値** |
| `""` | **0** | **いまの値** |
| `"abc"` | **0** | **いまの値** |
| `9` | 9 | 9 |
| `0` | 0 | **0**（はっきり0と言われたら0にする） |
| `99` | 10 | 10 |

**「0にする」は管理アプリから明示的に行うこと。**
通信の行き違いで0になってはいけない。

### ② 会員が無ければ、行を作らない

GAS は `ensureUserRowFromActivity_` で**新しい行を足す。**
サーバーでは足さず、「会員情報が見つかりません」と返す。

**無い会員に特典だけ書くと、名前も電話番号も生年月日も無い行ができる。**
2026-08 に「入れないお客様」が21名いた原因が、まさにこの形だった。

**行を作るのは `updateUser` の仕事**にする。

## 試した結果

    何も送らない       stamps=7  （守られた）
    null            stamps=7  （**GASなら0**）
    空文字           stamps=7  （**GASなら0**）
    "abc"           stamps=7  （**GASなら0**）
    9               stamps=9
    0               stamps=0  （はっきり0なら0にする）
    99              stamps=10 （上限）
    知らない会員      エラー・**行は増えない**（154→154）

実データは触っていない。試験用の会員を作り、最後に消した。

---

## ⑤の進み具合

| | |
|---|---|
| `syncUserDeviceSession` / `removeUserDeviceSession` | **済** |
| `syncUserRewardStatus` | **済** |
| `updateUser` | これから（**新規登録も兼ねる**） |
| `drawRewardGacha` | これから（**ロック**） |
| `recoverAccount` / `resetForgottenPasscode` | **最後** |


---

# ⑤の3つ目：`updateUser`（2026-08-23）

**作る前に列を点検したら、取り込みの取りこぼしが2つ見つかった。**
「列を点検してから作る」の決まりが、そのまま効いた形。

## 取りこぼし① 通知の届け先（8列目）— **これが一番まずい**

GAS の `getPushUsers()` は、この列の値を `subscription:` として配信に渡している
（管理者・お客様.js 8227行）。**配信の宛先そのもの。**

サーバーには真偽値の `push_enabled` しか無かった。さらに、最初の取り込みは
`真偽()` を通していた。

    真偽("true")      → True
    真偽("abc123...") → **False**   ← OneSignal の購読ID

**購読IDを持つ方は、全員オフに落ちていた。**
サーバーで通知オンは 153名中 **37名**しかない。

> **このまま切り替えていたら、お知らせが誰にも届かなくなっていた。**
> 気づけたのは、endpoint を作る前に列を突き合わせたから。

## 取りこぼし② メモ（7列目）

管理アプリの会員一覧に出る、受付の覚え書き（GAS 7932行）。
**そもそも書き出していなかった。**

## 回収の道具

| | |
|---|---|
| `gas/落ちた2列を書き出す.js` | 会員ID・メモ・届け先の**3列だけ**読んで書き出す |
| `落ちた2列を取り込む.py` | `--下見` で差分を見せてから入れる |

`Member` に `push_subscription` / `memo` を追加（migration 0003）。

**手順（要・実行）**

1. GASで `落ちた2列の下見()` → 「通知の届け先あり」が37名を大きく超えるはず
2. `落ちた2列を書き出す()` → ドライブからダウンロード
3. `落ちた2列を取り込む 落ちた2列.json --下見` で差分を確認
4. よければ `--下見` を外す
5. **JSONを消す**（メモが入っている）

---

## `updateUser` 本体

GAS の `handleUpdateUser`（6759行）と同じ。**会員を作るのは、この口だけ。**

JSON では `undefined` の項目は送られてこないので、**キーがあるか**で判断する
（GAS の `data.x !== undefined` と同じ）。

    キーが無い       → いまの値のまま
    キーがあって ""  → **空にする**（はっきり消したいということ）

### GASと違うところ（3つ）

1. **パスコードはハッシュで保存する。**GASは平文で表に書いている。
   平文をやめるのが、この移行の目的の1つ。照合は `check_password`。
2. **スタンプは、読めない値で0にしない**（`特典をそろえる` と同じ理由）
3. **行ロックを取る。**GASは読んで書くだけで、同時に届くと片方が消える

### 試した結果（手元のSQLite）

    新規登録         「山田 　太郎」→ 山田太郎（空白が消えた）
                    「ヤマダ タロウ」→ やまだたろう（ひらがなに寄った）
                    パスコード … 平文でない・照合できる
                    会員が1名だけ増えた
    電話だけ送る      住所も生年月日も**残った**
    届け先 = 購読ID   オン
    届け先 = false    オフ
    届け先 = "true"   オン
    スタンプ null/""/"abc" → **7のまま**（GASなら0）
    スタンプ 99      → 10
    お名前が空白だけ   → 断る・**いまの名前は無事**
    会員IDなし        → 断る

---

## 見つけたが、直していない不具合

**お客様アプリは、プロフィール保存のたびに `memo: ''` を送っている**
（`app.js` 7568行）。GAS はそれで上書きする。

> **受付の覚え書きが、お客様が自分の情報を保存するたびに消える。**

サーバーも同じ動きにしてある。**ここだけ直しても直らない**ため
（GASが動いている間は消え続ける）。正しい直し方は、アプリ側で `memo` を
送るのをやめること。**要相談。**

`落ちた2列の下見()` の「メモあり」が少なければ、すでに消えた後かもしれない。

---

## ⑤の進み具合

| | |
|---|---|
| `syncUserDeviceSession` / `removeUserDeviceSession` | **済** |
| `syncUserRewardStatus` | **済** |
| `updateUser` | **済**（手元で確認・サーバー未反映） |
| `drawRewardGacha` | これから（**ロック**） |
| `recoverAccount` / `resetForgottenPasscode` | **最後**（入り口） |


### 本番PCでの試験（2026-08-23・実際の通信で）

控えを取ってから実施（`backups/mayumi-20260823-updateuser.dump` 662KB）。

    新規登録        「山田 　太郎」→ 山田太郎 / 「ヤマダ タロウ」→ やまだたろう
                   パスコード … 平文でない・照合できる
                   生年月日 1990-05-03 / 登録経路 新規登録 / 会員 153→154
    電話だけ送る     住所・生年月日・**パスコードも残った**
    届け先=購読ID   オン / false → オフ / "true" → オン
    スタンプ null/""/"abc" → **7のまま** / 99 → 10
    お名前が空白だけ → 断る・いまの名前は無事
    会員IDなし      → 断る
    メモ           電話だけ送っても残る / memo="" で消える（GASと同じ）
    片付け後        会員 153名（元どおり）

**実データは無傷。**

> **PC側の作業ツリーに、git に入っていない scp の写しが残っていた。**
> `views.py` は手元と1バイトも違わず、`writes.py` は cd1d88b と同一だった
> （SHA-256で確認）。消さずに `git stash` へ退避してから pull した。
> **今後、PCへ直接 scp しない。**git を通す。


---

# ⑤の4つ目：`drawRewardGacha`（2026-08-23）

スタンプが10個たまった方が、**1枚のカードにつき1回だけ**回せる。

## 取りこぼしがもう1つ見つかった：スタンプ達成日時（16列目）

`stampAchievedDate`。**書き出してすらいなかった。**

これは**特典の有効期限（獲得＋1か月）の基準**になる。
無いまま切り替えると、有効期限が「**回した日**＋1か月」にずれる。
お客様アプリも `stampAchievedDate` を持っていて、端末とサーバーで新しいほうを
採る作りになっている（`app.js` 1806行）。

`Member.stamp_achieved_at` を追加（migration 0004）。
回収の道具は `落ちた2列…` を **`落ちた列…`（3列）** に広げた。

> **これで、取り込みの取りこぼしは3列。**
> メモ(7) / 通知の届け先(8) / スタンプ達成日時(16)。
> **3つとも、endpoint を作る直前の列点検で見つかっている。**
> 作る前に列を突き合わせる決まりが、そのまま効いている。

## 二重に引かせない仕組み

GAS は `LockService.getScriptLock()` で**スクリプト全体**を止める。
こちらは**その会員の行だけ**を止める。他のお客様の通信は止まらない。

ただし、**守りの本体はロックではない。**

    いまのカード番号と同じ番号の特典が、すでにあるか？
      → あれば **引かずに、その特典をそのまま返す**（alreadyDrawn）

通信が2回届いても、2つ目は「もう引いてあります」になるだけ。
ロックは、この判定と書き込みの間に割り込まれないようにするためのもの。

## 有効期限は JS の `setMonth(+1)` に合わせた

JS は、はみ出した日数を翌月へ送る。

    1/31 → 2/31 は無いので **3/3**
    3/31 → 4/31 は無いので **5/1**

「月末に丸める」ほうが自然に見えるが、**そうするとGASと1〜3日ずれる。**
お客様に見える日付なので、GASに合わせた。

## 試した結果

    スタンプ9個      → 引けない・特典0件
    10個            → D賞 よもぎ入浴剤 2個
                      獲得 2026-07-31 14:30 / 期限 2026-08-31 14:30（正しい）
    同じカードで2回目  → **alreadyDrawn・同じ特典・増えない**
    6回たたく        → **1件のまま**
    カードが2枚目に   → また引ける（2件）
    会員がいない      → 断る・**行は作らない**
    会員IDなし        → 断る

    確率（20万回）
      2026-07  A 5.07 / B 15.00 / C 30.10 / D 49.82   （設定 5/15/30/50）
      2026-08  A 4.98 / B 19.99 / C 35.01 / D 40.03   （設定 5/20/35/40）
      2026-09  A 4.89 / B 20.08 / C 35.01 / D 40.03   ← **設定が無いので8月を引き継ぐ**

月が変わった瞬間に賞が消えないこと（前の月を引き継ぐこと）も確認した。

---

## ⑤の進み具合

| | |
|---|---|
| `syncUserDeviceSession` / `removeUserDeviceSession` | **済** |
| `syncUserRewardStatus` | **済** |
| `updateUser` | **済** |
| `drawRewardGacha` | **済**（手元で確認） |
| `recoverAccount` / `resetForgottenPasscode` | **残り**（入り口・最後） |


### 本番PCでの試験（2026-08-23・実際の通信で）

控えを取ってから実施（`backups/mayumi-20260823-gacha.dump` 662KB）。
**本番の設定そのまま**（2026-05〜08の4か月分）で確認。

    スタンプ9個      → 断る・特典0件
    10個            → D賞 よもぎ入浴剤 お試し2個   ← 本番の8月の賞と一致
                      獲得 2026-07-31 14:30 / 期限 2026-08-31 14:30
    同じカードで6回   → **1件のまま・同じ特典を返す**
    カードが2枚目に   → C賞・2件
    会員がいない      → 断る・**会員は増えない**
    会員IDなし        → 断る
    片付け後          会員153名（元どおり）

**実データは無傷。**

