# 移行設計 — スプレッドシートから関係データベースへ

[データ棚卸し](データ棚卸し.md)の結果をもとに、いまの 17＋8 シートを何テーブルに落とすかをまとめたもの。
**まだ決めていない項目は最後の「決めていただきたいこと」にまとめてある。**

- 前提: PostgreSQL（KEM_DDENKI と同じ構成を借りる）
- 方針: **お客様の入り直しをゼロにする**（[README](../README.md) の「ログインの仕組み」を保つ）

---

## 全体像

```
                    ┌──────────────┐
                    │   members    │ ← すべての中心。172名
                    │  （会員）     │
                    └──────┬───────┘
                           │ member_id
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼──────┐  ┌────────▼───────┐  ┌───────▼────────┐
│ measurements │  │   responses    │  │ stamp_events   │
│  （測定）     │  │  （回答）       │  │ （スタンプ履歴） │
└──────────────┘  └───────┬────────┘  └────────────────┘
                          │ response_id
                  ┌───────▼────────┐
                  │ ticket_analyses│
                  │  （回数券分析） │
                  └────────────────┘

会員に紐づかないもの
  surveys / news / calendar_events / menus / products
  revenue_records / faqs / supplier_prices / audit_logs / backups
```

---

## テーブル一覧

### 中心

| テーブル | 元 | 行数 | 備考 |
|---------|-----|------|------|
| **members** | 会員データ | 172 | 33列 → **26列**（未使用7列を落とす） |
| **measurements** | 測定履歴 | 42 | 会員に **ID で** 紐づけ直す（後述） |
| **responses** | 回答一覧 | 0 | 回答本体は `answers` を JSONB で持つ |
| **ticket_analyses** | 回数券分析結果 | 16 | responses への FK |
| **stamp_events** | スタンプ履歴JSON | — | JSON を行に開く（147名分） |
| **reward_events** | 特典履歴JSON | — | 同上（165名分） |

### コンテンツ

| テーブル | 元 | 行数 |
|---------|-----|------|
| news | ブログ・お知らせ | 94 |
| calendar_events | カレンダー | 116 |
| menus | MENUS | 13 |
| products | 商品マスタ | 9 |
| categories | カテゴリマスタ | 7 |
| faqs | APP_SUPPORT_FAQ | 46 |
| push_notices | PUSH_NOTICES | 96 |
| surveys | SURVEYS_JSON（プロパティ） | 2 |
| bijiris_posts | ビジリス通信 ＋ 添付 | 0 |

### 記録・管理

| テーブル | 元 | 行数 |
|---------|-----|------|
| revenue_records | MENU_REVENUE ＋ PRODUCT_REVENUE | 175 |
| supplier_prices | 管理マスタ（仕入値） | 22 |
| audit_logs | ADMIN_AUDIT_LOG | 9,545 → **762**（自動処理ぶんを除く） |
| backups | BACKUP_LOG | 130 |

### 作らないもの

| 元 | 理由 |
|----|------|
| 注文管理 | 0件。**作らないと決定**（A） |
| SUPPORT_CHAT_LOG | 0件。同上 |
| ADMIN_TEMPLATES | 0件。同上 |
| 測定記録_会員別 | 測定履歴から自動生成しているだけ |
| 会員別まとめ（別ファイル12シート） | 同上。移行後は画面か出力で作る |
| DASHBOARD_SNAPSHOT | 作り置きのJSON。DBなら都度集計で足りる |
| 各アンケートのシート（施術後・計測時） | responses の見せ方違い。ビューか画面で作る |

---

## members の列（26列）

| 列 | 型 | 埋 | 備考 |
|----|----|----|------|
| id | text PK | 172 | **いまの会員IDをそのまま使う**（変えない） |
| created_at | timestamptz | 172 | 登録日時 |
| name | text NOT NULL | 168 | ※空4行は削除済みなので実質172 |
| kana | text | 34 | |
| phone | text | 131 | |
| birthday | date | 125 | |
| address | text | 113 | |
| avatar_url | text | 15 | |
| push_enabled | boolean | 57 | |
| status | text | 2 | ご状況 |
| stamp_count | integer | 165 | |
| stamp_card_number | integer | 165 | |
| last_stamp_at | timestamptz | 126 | 重複2列を1本化 |
| **passcode_hash** | text | 137 | **平文をやめる（後述）** |
| **passcode_salt** | text | — | 同上 |
| password_hash / password_salt | text | 2 | 旧方式の2名。移行後に統合 |
| role | text | 2 | 管理者判定 |
| transfer_code / transfer_code_issued_at | text / timestamptz | 0 | 引き継ぎ用。使う |
| device_sessions | jsonb | 161 | |
| deleted_at | timestamptz | 0 | 論理削除 |
| merged_into_id | text | 0 | 統合先 |
| registration_source / _detail / _updated_at | text | 136 | |
| last_online_at | timestamptz | 11 | |
| bijiris_registered | boolean | 4 | |

**落とす列（7つ・全件空）**: メモ / スタンプ達成日時 / 削除状態（deleted_at に統合）/ 削除日時（同）/ 統合先会員ID は残す（merged_into_id）… 実際に落とすのは **メモ・スタンプ達成日時・引き継ぎコード発行日時の重複分・最終スタンプ取得日の重複分**。

---

## 移行で直す3つのこと

### ① パスコードが平文で保存されている

いまの `パスコード` 列は **数字がそのまま入っている**（137名分）。スプレッドシートを開ける人は全員の暗証番号を読める状態。

移行時に**塩＋ハッシュに変換**する。移行スクリプトが平文を読める最後の機会なので、ここでやるのが一番きれい。

**お客様への影響はゼロ。**いまのパスコードがそのまま使え、入り直しも不要。

### ② ビジリスと会員が「お名前の文字列」で紐づいている

測定履歴の `会員番号` は 43行中 **1行しか埋まっていない**。実際の紐づけはお名前の一致頼み。
「山谷 → 山谷未央」の改名で専用スクリプトが必要になったのはこのため。

移行時に **member_id で紐づけ直す**。改名しても記録が切れなくなる。

> 移行手順: お名前で突き合わせ → 一致しないものを一覧で出す → 目で確認して割り当て。
> 42件しかないので確認は現実的。

### ③ 同じ意味の列が2つある

`最終スタンプ取得日`(126) と `最終スタンプ取得日時`(122)、`引き継ぎコード発行日時` など、
シートを継ぎ足してきた結果の重複。移行時に1本化する。

---

## 入り直しをゼロにするための約束

移行後も**この3つを必ず保つ**。

| | 保つもの |
|---|---|
| 1 | `ADMIN_TOKEN_SECRET` と札の形式（`base64url({t,u,exp,sig})` / HMAC-SHA256）→ 発行済みの札が通る |
| 2 | 会員ID → スタンプ・回数券・測定履歴が続く |
| 3 | 画面のURL（GitHub Pages のまま）→ ホーム画面のアイコンが生きる |

切替前に**「GASが作った札を新しい側が同じと判定するか」を実データで突き合わせる試験**を通す。
これが合わない限り切り替えない。

---

## 写真をどうするか

いま写真は Google ドライブにあり、シートには URL が入っている（計測写真・ビフォー/アフター）。

**当面はドライブのままにする。**DBには URL と ファイルID だけを持つ。

- 移行対象が減り、切替の risk が小さい
- 院長がドライブから直接見られる運用が変わらない
- 将来まとめて移したくなったら、URL列を差し替えるだけで済む

---

## 決まったこと（2026-08-14）

### A. 使っていない3機能 → **作らない**

注文管理・サポートチャット・お知らせテンプレートは、いずれも一度も使われていない（0件）。
新しい方には作らない。必要になったらそのとき足す。

→ **3テーブル・61列ぶんの設計と画面が不要になる。**

### B. 操作履歴 → **人の操作ぶん（約760行）だけ移す**

中身を数えた結果、**9,562行のうち92%が自動処理の記録**だった。

| 種別 | 件数 | 内容 |
|------|------|------|
| **syncUserDeviceSession** | **7,993** | 端末の記録合わせ。アプリが勝手に行う |
| **syncUserRewardStatus** | **807** | 特典状態の同期。同上 |
| recoverAccount | 140 | パスコード復旧 |
| updateUser | 137 | 会員情報の編集 |
| addBlog | 69 | お知らせ追加 |
| saveMenuRevenueRecord | 60 | 売上記録 |
| updateAdminRewardStatus | 59 | 特典の手動変更 |
| deleteUser | 42 | 会員削除 |
| resetForgottenPasscode | 39 | パスコード再設定 |
| addCalendar / updateBlog / deleteOrders | 38 / 30 / 30 | |

- 期間: 2026-05-14 〜 2026-08-14（93日）
- 結果: 成功 9,286 / 失敗 276
- 操作者: 全件「管理者」（誰がやったかは記録されていない）

**上の2種を除いた約760行だけを移す。**93日ぶん全部でもこの量。

> **ついでに直したいこと**: この2種は今後も記録しない方がよい。
> 93日で8,800回の書き込みは、シートを太らせるだけで誰も見ない。
> 「操作者」が常に「管理者」なので、監査の役にも立っていない。

### C. アンケートの設問 → **JSONB のまま持つ**

いまの作りに近く、設問を変えてもテーブル変更が要らない。

### D. 移す単位は → **アプリ単位ではなく「表」単位**（2026-08-15 に方針変更）

当初は「管理側だけ移す」としていたが、これは**同じ情報が2か所にできる**危険な形だった。
管理アプリが PostgreSQL に書き、お客様アプリがスプレッドシートに書くと、
どちらが正しいか分からなくなる。

**正しい切り方は「表」単位。**表ごとに住む場所を1つに決め、済んだ表から減らしていく。
詳しくは [進め方](#進め方表を1つずつ引っ越す) を参照。

---

## 下見の結果（2026-08-15 08:53 再実施）

読み取り専用で実データを1件も変えずに確認した。道具は `bijiris/gas/移行の下見.js` の `移行の下見をする()`。

### 会員 — きれいだった

| | |
|---|---|
| 会員の行 | **158**（不要な行とテスト・重複を整理したあと） |
| IDが空 / 重複 | **0 / 0** |
| お名前が空 | **0** |
| **同姓同名** | **0組** ← 名前での突き合わせが成立する |
| パスコードが平文 | **130名** → 移行時にハッシュへ |
| すでにハッシュ方式 | 1名 |
| 入り口に入れない | 28名 |

### ビジリスの記録を会員IDに結べるか

| 表 | 件数 | 結べる | 見つからない |
|----|------|--------|------------|
| 測定履歴 | 42 | **42** | **0** |
| 回数券分析結果 | 15 | **15** | **0** |
| 回答一覧 | 0 | — | — |

**突き合わないものはゼロ。すべて会員IDに結べる。**

> 当初は「小瀬村真理子」さん（計5件）が突き合わなかった。会員登録が無いと判断したが、
> **実際は「コセムラマリコ」とカタカナで登録されていた**。院長にご本人と確認いただき、
> 新規登録ではなく**お名前の付け替え**で解決した（2026-08-15）。
> 新規登録していたら二重登録になり、記録が分かれていた。

### 操作履歴

| | |
|---|---|
| いまの記録 | 9,562行 |
| **移す** | **762行** |
| 移さない（自動同期ぶん） | 8,800行 |

### そのまま移せる表

news 94 / calendar_events 116 / menus 13 / products 9 / categories 7 /
faqs 46 / push_notices 96 / revenue_records 175 / supplier_prices 22 / backups 130

---

## 下見でわかった設計の変更点

**measurements.member_id は NULL を許す。**

いまは全件が会員に結びつくが、**アンケートの送信には会員登録が要らない**ため、
「記録はあるが会員ではない方」はいつでも発生しうる。会員IDを必須にすると、
その方の記録が移せなくなる。

```
measurements
  member_id   … NULL 可（会員登録がない方）
  customer_name … 常に残す（member_id が無いときの手がかり）
```

会員登録されたら member_id を後から埋められるようにしておく。

---

## 進め方（表を1つずつ引っ越す）

### 置き場所

**KEM_DDENKI のPC（`desktop-rmsk0vg`）に Django + PostgreSQL を置き、Tailscale Funnel で公開する。**

Funnel は公開のHTTPSなので、**Googleのサーバー（GAS）からも届く**。
以前「VPSが必要」と書いたが、それは誤り。

> **前提条件（移行前に必ず済ませる）**
> このPCは Docker Desktop が「サインイン時に自動起動」の設定になっており、
> 再起動後に誰かがWindowsにサインインするまで PostgreSQL が上がらない。
> 停電から復電しても、翌朝まで止まったままになりうる。
>
> 1. BIOS/UEFI で「電源復帰時に自動起動」（`Restore on AC Power Loss` → Power On）
> 2. Windows の自動サインイン（`netplwiz`）
> 3. スリープしない設定
> 4. 併せて BitLocker か画面ロックで、目の前の人には鍵をかける
>
> **3つ揃わないと意味がない。**①が無ければ②③は動かない。

### GAS は窓口として残す

```
お客様のアプリ ─┐
                ├→ GAS ─┬→ Funnel → PostgreSQL（引っ越し済みの表）
管理アプリ ─────┘        └→ スプレッドシート（まだの表）
```

| 役割 | |
|------|---|
| GAS | **窓口。**アプリから見た話し相手は最後まで変わらない |
| PostgreSQL | 引っ越した表の**唯一の正** |
| スプレッドシート | まだの表の**唯一の正**。引っ越した表は書き出し用の控えに変わる |

**どの表も、住んでいる場所は常に1つだけ。**だからズレようがない。

これにより、
- お客様の**入り直しはゼロ**（札もURLも触らない）
- 1回の切り替えは**表1つ分**。おかしければその表だけ戻す
- 約160の窓口を一度に書き直さなくてよい

### 表の分け方の原則

**「1人に1つ」か「1人に何個も」か**で決める。

| | 例 | 置き場所 |
|---|---|---|
| 1人に1つ | 氏名・電話・住所・パスコード・スタンプ数 | **会員台帳（members）** |
| 1人に何個も | 計測・アンケート回答・回数券分析 | **別々の表** |

計測履歴を会員台帳に入れられないのは、1人が何回も測るため。

> **会員登録情報は、必ず1か所だけに置く。**
> ビジリス側にも氏名を持つと、まゆみ側と表記がズレて別人になる。
> 「小瀬村真理子 / コセムラマリコ」がまさにこれだった。
>
> **スタンプは2種類ある。**まゆみのスタンプカード（会員台帳）と、
> ビジリスの回数券（ビジリス側）は別物。混ぜない。

### 引っ越す順番

**止まっても困らない表から。**会員データは最後。

| # | 表 | 件数 | 止まったときの影響 |
|---|----|------|------------------|
| 1 | **測定履歴** | 42 | ビジリス管理で見られないだけ。**お客様に影響なし** |
| 2 | 回数券分析結果 | 15 | 同上 |
| 3 | アンケート回答 | 0〜 | 送信できない |
| 4 | お知らせ・カレンダー・商品 | 200超 | 表示できない |
| 5 | 売上・仕入 | 197 | 院内のみ |
| 6 | **会員データ** | 158 | **誰もログインできない** |

①を移したあと **2〜4週間動かし、実際に何時間止まったかを測ってから**、
会員データを移すか判断する。止まる頻度が高ければ VPS（月500〜1,000円）へ切り替える。
Django ごと引っ越すだけなので、あとからでも難しくない。

### 表1つの引っ越し手順（毎回これを繰り返す）

1. Django にその表のモデルを作る
2. **読み取りだけ**のAPIを立て、GASから読めることを確かめる
3. スプレッドシートから中身を写す（件数を突き合わせる）
4. GASの**読み取り**をAPIに切り替える。書き込みはまだシート
5. 1週間ようすを見る
6. GASの**書き込み**もAPIに切り替える
7. シート側は「夜間に書き出す控え」に変える（手で書いても反映されない旨を明記）

**問題が出たら、その表だけ手順4の前に戻す。**他の表には影響しない。

---

## 進んだところ（2026-08-17 時点）

| | 状態 |
|---|---|
| サーバーを立てる | **済**。自宅PCの Docker（`server-db-1` / `server-web-1`） |
| 外に公開する | **済**。`mayumi-api.tail8efe0d.ts.net`（専用ノード・ポート番号なし） |
| 落ちたら知らせる | **済**。15分おき。約30分止まったらメール |
| 控えを取る | **済**。毎日3時＋Googleドライブへ。**戻せることを実証済み** |
| **会員データ 153名** | **済**。突き合わせ0件・パスコード129名を実際に照合 |
| **測定履歴 42件** | **済**。全件が会員IDに結びつく（迷子0件・10名・計測値の空0件） |
| **お知らせ 95件** | **済**。公開94・削除済み7・画像88 |
| **カレンダー 143件** | **済**。公開143・削除済み21・画像30 |
| **メニュー 13件** | **済**。公開10・画像11枚（配列のまま） |
| **商品 9件** | **済**。公開8・在庫は0と空欄を区別 |
| **カテゴリ 7件** | **済**。種別は お知らせ4・通知2・ブログ1 |
| **使い方FAQ 46件** | **済**。全件公開・行2〜47が連番・カテゴリ19種の合計が46 |
| **プッシュ通知 96件** | **済**。行2〜97が連番・日時/通知ID/送信結果とも42件で食い違い0 |
| **売上 177件** | **済**。2枚を1表に。合計2,037,090円が元と1円も違わない |
| **仕入 22件** | **済**。商品9件すべてに原価が1件ずつ結びついた |
| 回答・操作履歴・控え | 未着手 |

> 設計書の件数（お知らせ94・カレンダー116）は実物と違った。
> 実物は95行・143行で、カレンダーの116は削除済みを除いた数だったと思われる。
> **移す前に必ず `表の下見()` で実物を見ること。**

### FAQ・プッシュ通知の下見で分かったこと（2026-08-17）

**プッシュ通知は、見出しが13列なのに実物は15列あった。**
`ensurePushNoticeSheetStructure_` が作るのは13列で、14・15列目の
削除状態・削除日時はあとから汎用の削除の仕組みが足したもの。
どちらも0件だが、コードだけ見て表を作れば2列を落としていた。

**日時が96件中42件しか埋まっていなかった。**
「古い行だから空なのだろう」と決めずに `表の内訳()` を作って数えたところ、

| ステータス | 件数 | 日時 | 通知ID | 送信結果 | 送信件数 |
|---|---|---|---|---|---|
| 自動送信済み | 90 | 40 | 40 | 40 | 90 |
| 送信済み | 6 | 2 | 2 | 2 | 6 |

日時・通知ID・送信結果の3つが**揃って埋まるか揃って空**で、
ステータスとは関係が無かった。OneSignal から通知IDが返ってきた
配信がこの42件。**空欄は空欄のまま移す。**埋めると、
届いていない通知が届いたことになる。

このため**日付を空行の判定に使えない。**タイトル（96件すべて）で判定する。
日付で判定していたら54件が黙って落ちていた。

**送信対象詳細（6件）の中身も確かめた。**会員の一覧（JSON）が
入りうる列なので、書き出す前に実際の値を見た。入っていたのは
「テスト送信:デモ2」「確認送信:デモ2」というラベルだけで、
個人情報は含まれていなかった。**将来ここに会員一覧が入ったら、
書き出したJSONは個人情報を含む。**そのときは
`書き出しJSONを片付ける()` の要注意扱いに加えること。

### 売上の下見・書き出しで分かったこと（2026-08-17）

**実物は 112＋65＝177件。**設計書の175件と2件ちがう。
これで4つ目の食い違い（お知らせ94→95、カレンダー116→143、
売上175→177、控え130→139）。**設計書の件数は当てにしない。**

**2枚で同じ意味の列の名前が違う。**寄せるときに取り違えないこと。

| ここでの名前 | MENU_REVENUE | PRODUCT_REVENUE |
|---|---|---|
| name | メニュー種別 | 商品名 |
| quantity | 件数 | 個数 |
| unit_price | 単価 | 単価 |
| unit_cost | 原価単価 | 原価 |

「原価単価」と「原価」はどちらも1つあたりの値段。合計ではない。

**行番号だけでは鍵にならない。**2枚とも2行目がある。種別と組で一意にした。
行番号だけを鍵にしていたら、どちらか片方の記録が丸ごと上書きされていた。

**メニュー112件は原価単価がすべて0。**空欄ではなく実際に0で、
「本当に原価0円」だと院長に確認済み。教室・施術は仕入れが無い。
0なら粗利＝売上、空欄なら粗利は出せない（None）として区別する。

**単価は整数ではなかった。**書き出したJSONを本番へ入れる前に検査したら、
商品の売上合計が 348,090.0 と小数になっていた。原因は

    商品32〜35行 天然だし調味粉  数=3  単価=2331.6666…  メモ「3個購入集計 4人分」

3個で6995円のまとめ買いを1個あたりに割った値。`int()` で受けると
2331 になり、3×2331=6993 で1行あたり2円、4行で8円が消える。
**金額を勝手に減らさない。**`DecimalField(小数6桁)` にして、
金額は「掛けてから円に四捨五入」する。先に単価を丸めると桁が落ちる。

    2331.666667 × 3 = 6995.000001 → 6995円（元どおり）

取り込みの集計に「割り切れない値 N件」を出すようにした。
次に同じことが起きたときに、気づかず通り過ぎないため。

### 仕入を移して分かったこと（2026-08-17）

**22件のうち、商品マスタと結びついたのは9件。**残り13件は名前が
商品マスタに無いもので、3種類に分かれた。

| 種類 | 行 | 中身 |
|---|---|---|
| テスト行 | 14〜19 | テスト1000 / テスト3 700 / テスト用3500×2 / テスト1100 / テスト1000 |
| 括弧が半角 | 10, 12, 13 | `よもぎ茶(50パック)` 1570円 など |
| その他 | 8, 11, 21, 22 | 冷え取りパット（2枚）800 / 石鹸350 / オケタニおんぶひも3000×2 |

**書き出しの時点で「同じ商品名が4件」と警告が出た。**
名前で結びつける作りなので、同じ名前が2行あるとどちらの仕入値か決まらない。
中身を見たところ、重複していたのは「テスト」「テスト用」
「オケタニおんぶひも」で、**いずれも商品マスタに無い名前**だった。
つまりどの商品の原価にも影響しない。実害なしで済んだ。

**院長の判断で22件すべてそのまま移した。**実物どおりに入れ、
結びついたかどうかだけを記録する。テスト行も残るが、商品には
繋がらないので原価計算には影響しない。片付けはあとでもできる。

結果、**商品9件すべてに原価が1件ずつ**、迷いなく結びついた。

| 商品 | 原価 |
|---|---|
| よもぎ茶（50パック） | 1,875円 |
| よもぎ茶（30パック） | 1,238円 |
| よもぎ入浴剤（10パック） | 1,155円 |
| よもぎ入浴剤（2パック） | 280円 |
| 石鹸（あこがれのきよら） | 470円 |
| 冷え取りパット（3枚セット） | 2,160円 |
| 冷え取りパット（1枚） | 800円 |
| 天然だし調味粉 | 1,490円 |
| クロス(25×25) | 810円 |

**10行目の `よもぎ茶(50パック)` 1570円 は、2行目の
`よもぎ茶（50パック）` 1875円 と括弧の全角・半角しか違わない。**
値は違う（1570 と 1875）。同じ商品の新旧かもしれないが、
「完全一致」の作りなので別物として扱われている。
**勝手に寄せない。**寄せると原価が変わる。気になるときは院長に確認する。

5列目に入っていた運用メモの実物:

> 注文管理シートでは、D列の「商品名」とここの「商品名」を照合して
> 「仕入値」を自動で書き出す数式（VLOOKUP）が入ります。
> ※新商品を追加する際は、必ずこちらにも商品名と仕入値を記入してください。

つまり**スプレッドシート側も名前で照合している。**サーバー側で
名前のまま持つ判断は、いまの運用と同じ考え方になっている。

### 操作履歴の下見で分かったこと（2026-08-17・移行はこれから）

**実物は 9,373行 × 7列・31種。**設計書は9,545行または9,562行と
書いていたが、実物は**減っている。**5つ目の食い違い。

| 列 | 中身 |
|---|---|
| 1. 日時 | 例: 2026-05-17T09:25:47+09:00 |
| 2. 種別 | 例: syncUserDeviceSession |
| 3. 結果 | 成功 / 失敗 |
| 4. 対象 | 例: 🌿ミネラルワークショップ 1日目🌿 |
| 5. 概要 | **種別によって埋まり方がまったく違う**（下記） |
| 6. 操作者 | 全件「管理者」 |
| 7. 詳細JSON | 例: `{"request":{"type":"syncUserDeviceSessio…` |

全9,373行を数えた内訳（多い順）:

| 種別 | 件数 | 割合 |
|---|---|---|
| **syncUserDeviceSession** | **7,788** | 83.1% |
| **syncUserRewardStatus** | **791** | 8.4% |
| updateUser | 155 | 1.7% |
| recoverAccount | 140 | 1.5% |
| addBlog | 67 | 0.7% |
| saveMenuRevenueRecord | 61 | 0.7% |
| updateAdminRewardStatus | 59 | 0.6% |
| resetForgottenPasscode | 49 | 0.5% |
| deleteUser | 42 | 0.4% |
| addCalendar | 40 | 0.4% |
| deleteOrders / updateBlog / drawRewardGacha | 30 / 29 / 27 | |
| saveProductRevenueRecord | 16 | |
| updateCalendar / mergeUsers / deleteRow | 13 / 13 / 10 | |
| その他14種 | 各1〜8 | |

**上2種を除いた「人の操作」は 794件**（9,373 − 8,579）。

**「概要」列は、200行だけ見ると空に見える。**

    先頭199行だけ  … 概要 0/199件   ← 空の列に見える
    全9,373行     … 種別によって違う

| 種別 | 概要の埋まり方 |
|---|---|
| syncUserDeviceSession | 115/7,788（ほぼ空） |
| updateUser | 0/155（全部空） |
| recoverAccount | 114/140 |
| deleteOrders | 30/30（全部埋まっている） |
| mergeUsers | 13/13（同上） |

**200行の傾向で「この列は使われていない」と判断していたら、
実データを落としていた。**このため `表の下見()` の件数表示に
母数を付けた（「115/7788件」「**先頭199行だけを見た結果です**」）。
「◯件」とだけ出すと、シート全体の数だと読めてしまう。

### 控えの下見で分かったこと（2026-08-17・移行はこれから）

**BACKUP_LOG は139行×5列**（作成日時・種別・ファイル名・ファイルID・URL）。
設計書の130件と9件ちがう。

FAQ は46行×7列で全列46件、欠けは無かった。キーワードは読点区切りの
文字列のまま持ち、分ける規則（`[\n,、，/空白]`）を GAS と
サーバーの両方に同じものを書いた。**片方だけ直すと、答えの当たり外れがずれる。**

**会員データはサーバーに入ったが、正はまだスプレッドシート。**
サーバーは写しで、お客様の使い方は何も変わっていない。

引っ越す順番は「止まっても困らない表から」としていたが、会員データを先に
移した。写しである限り止まっても影響が無く、パスコードを平文でなくす効果が
先に得られるため。**正を移すのはこれから。**

積み残し:
- 控えのフォルダが、Webアプリを動かすアカウントと編集するアカウントで
  見え方が違う（保存はできている）。どちらのアカウントか要確認。
- 回数券スタンプの集計中は、お客様アプリに案内を出している。
  終わったら `bijiris/customer-app/app.js` の `集計中のお知らせを出すか_` を false に。

## 次にやること

**データ側の準備は終わっている。**突き合わないものはなく、いつでも移せる状態。

| # | やること | 誰が |
|---|---------|------|
| 0 | **いま developにある未公開の作業を先に片付ける**（ログイン一本化の実機確認 → main公開 → お知らせ） | 院長＋開発 |
| 1 | PCの自動復帰3点を設定する（[前提条件](#置き場所)） | 院長 |
| 2 | PCに Django を用意し、Funnel で公開する | 開発 |
| 3 | **測定履歴（42件）**を[手順](#表1つの引っ越し手順毎回これを繰り返す)どおりに移す | 開発 |
| 4 | 2〜4週間、止まった時間を測る | — |
| 5 | その実績を見て、会員データを移すか・VPSにするかを決める | 院長 |

> **0を飛ばさないこと。**developには60コミット分の未公開の変更があり、
> ログインの経路そのものを変えている。その上にデータベース移行を重ねると、
> 問題が起きたときにどちらが原因か分からなくなる。

### 移行前にやっておくとよいこと

- **「ビジリス」列を実態に合わせる。** 測定記録のある方は10名いるが、この列が立っているのは2名だけ。
  入口のアプリ一覧はこの列で出し分けているため、記録がある方にビジリスが表示されていない。
- **入り口に入れない28名。** うち16名は電話番号と生年月日があるので「お困りのとき」から自力で設定できる。
  残りは受付での対応が要る。
