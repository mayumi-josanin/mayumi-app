# 9/19(土) 会員の表を切り替える — 当日の手順

**2026-09-15 作成。**設計と経緯は [会員の表を移す](会員の表を移す.md)。
このページは**当日、上から順に実行するためだけ**のもの。判断の根拠は書かない。

- 開始 **10:00**。受付の予定が無いことを前に確かめる（スタンプを入れている最中に切り替えない）
- 切り替えは **`SERVER_TABLES` に `member` を1回で足す**（3段階には分けない。理由は設計書）
- 戻す期限 **9/21(月) の夜**。戻すのは `member` を消すだけ

サーバーPCの作業は Git Bash の補助 `~/member_cutover.sh` で行う（ASCII だけ。何度実行してもよい。
元は `server/scripts/member_cutover.sh`。変えたら scp で置き直す）。

    ssh rmsk '& "C:\Program Files\Git\bin\bash.exe" -lc "bash ~/member_cutover.sh <命令> [ファイル]"'

---

## 0. 前日まで（9/18）

- [ ] `docs/design/会員の表を移す.md` の「9/19 までに済ませておくこと」が全部 [x]
- [ ] GAS のデプロイが最新（`clasp deployments` で いつものID が最新の番号）
- [ ] サーバーが動いている（`bash ~/member_cutover.sh count` が数を返す）
- [ ] GAS エディタで `サーバーへの道を確かめる()` → 「両方とも届いています」
- [ ] `渡している表を見る()` → `news,category,menu,product,calendar,order,push`（member は**まだ無い**）

## 1. パスコードの点検（GAS エディタ・読むだけ）

- [ ] `パスコードの下見()` → 「短くなっている」が **0名**
  - 0名でなければ `パスコードの0を戻す()` → もう一度下見 → 0名
  - **表を直してから書き出す**（順番を逆にすると古いハッシュが残る）

- [ ] `電話番号の0を戻す()` の下見 → 直す（9/15 の稽古で3件、シート側の先頭の0が落ちていた。台帳との突き合わせを0にするため）

## 2. 書き出す（GAS エディタ）

- [ ] `会員データの下見()` → 名数と「除いた行」を控える（9/15 は 167名 / 0件）
- [ ] `会員データを書き出す()` → ドライブに JSON（**平文パスコード入り**）
- [ ] `会員ごとの印の下見()` → 件数を控える
- [ ] `会員ごとの印を書き出す()` → ドライブに JSON
- [ ] 2つの JSON をサーバーPCの `~/projects/mayumi-app/server/` に置く（scp。名前は ASCII に直す: `members.json` / `marks.json`）

## 3. 控えを取る

- [ ] サーバー: `bash ~/member_cutover.sh backup` → `backups/mayumi-<日時>-cutover.dump` の大きさを控える
- [ ] 控えをドライブにも写す（9/8 と同じ場所）
- [ ] スプレッドシート: ファイル → コピーを作成 → 名前に「切替前 9/19」

## 4. 取り込む → 突き合わせる（サーバー）

- [ ] `bash ~/member_cutover.sh preview members.json` → 新しく/変わる/変わらない の内訳を控える
- [ ] `bash ~/member_cutover.sh import members.json`
- [ ] `bash ~/member_cutover.sh marks-preview marks.json` → 内訳を控える
- [ ] `bash ~/member_cutover.sh marks-import marks.json`
- [ ] `bash ~/member_cutover.sh compare members.json` → **食い違い 0**（件数だけでなく全項目。9/15 は 167名・パスコード153名照合）
  - 食い違いが出たら**ここで止める。**切り替えない。理由が分かるまで先へ進まない
- [ ] `bash ~/member_cutover.sh count`
  - 通知の届け先が **0 でない**こと（9/8 は 42名。シートの69名との差は `"false"` 27名ぶんで正しい）
  - 「サーバーにだけいる会員」= 9/8 時点で MYM-5344 の1名。**院長に確認してから**扱いを決める（消さない）
- [ ] `bash ~/member_cutover.sh check` → GAS の `入れるか確かめる()` と数を見比べる（9/15 のサーバー側:
  パスコード無し 15名＝受付が先に作った行でこれから付ける方・旧方式のみ 0名・生年月日無し 25名・
  生年月日も電話も無し 16名・お名前空 0名・LINE重複 0・管理者 2名。**大きくずれていたら止める**）
- [ ] `bash ~/member_cutover.sh shred members.json` と `shred marks.json`（平文パスコードを残さない）

## 5. 切り替える（GAS のスクリプトプロパティ）

- [ ] `SERVER_TABLES` を `news,category,menu,product,calendar,order,push,member` にする（**デプロイ不要**）
- [ ] `渡している表を見る()` → member が出る
- [ ] サーバー: `bash ~/member_cutover.sh live-on`（Django 管理画面の会員まわりの書き込みが通るようになる）
- [ ] 時刻を控える: 切り替え ____:____

## 6. 切り替え直後に確かめる（10分以内）

読むだけ・自分の端末で:

- [ ] `bash ~/member_cutover.sh probe` → 3つとも 200、web にエラー無し
- [ ] 旧管理アプリ → 会員管理 → 名数が 4. の数と合う（サーバーから読んでいる）
- [ ] 旧管理アプリ → 通知管理の配信対象の件数が 4. の届け先の数と合う
- [ ] お客様アプリを自分の端末で開く → 起動できる・スタンプが**減っていない**・端末の記録合わせが通る
- [ ] ビジリス app で会員が見つかる（`checkMemberToken` がサーバーを見ている）
- [ ] GAS `サーバーとのやり取りの失敗を見る()` → 新しい失敗が無い

書き込みを1件だけ（テスト会員で・受付の記録にならないもの）:

- [ ] テスト会員でログイン → 入れる
- [ ] テスト会員のプロフィール（住所など）を変えて保存 → 旧管理アプリの会員管理に出る
- [ ] **お客様の本物の記録では試さない**

## 7. 見守る（9/19 夜・9/20・9/21）

- [ ] 1日2回: `サーバーとのやり取りの失敗を見る()` と `bash ~/member_cutover.sh count "2026-09-19 10:00"`（切り替え後にサーバーが作った会員と操作履歴が並ぶ）
- [ ] `見張りを始める()` が動いていること（サーバーが落ちたらメール）
- [ ] 不具合が出たら **8. へ**。9/21(月) の夜を過ぎたら戻さず直す

## 8. 戻す（必要なときだけ）

- [ ] `SERVER_TABLES` から `member` を消す（デプロイ不要。数秒）
- [ ] サーバー: `bash ~/member_cutover.sh live-off`（Django 管理画面の会員まわりの書き込みを止める）
- [ ] `bash ~/member_cutover.sh backup`（戻したあとの控え。切り替え中に書かれた記録はこの中にある）
- [ ] `bash ~/member_cutover.sh count "2026-09-19 10:00"` に出る **切り替え後に作られた会員**（新規登録）と **行番号が空の操作履歴**（復元・パスコード再設定）を手でシートへ写す
  - 端末の記録合わせ・スタンプの同期は写さなくてよい（端末側に残っている）
- [ ] 写し終えたら院長に報告し、原因を調べてから再挑戦の日を決める

## 9. 9/23 のあと（当日にやらない）

- 予約システムのお客様を台帳へ取り込む（[予約のお客様を台帳へ入れる](予約のお客様を台帳へ入れる.md)）
- 会員管理・スタンプ・特典を Django の管理画面へ移す
